# PLAN — 연습에 쓸 수 있는 악보 출력까지 (2026-08-07 승인)

이 문서는 **P0~P8 단계의 실행 기록**이다 — 무엇을 어떻게 했고 무엇을 측정했는지.

- **무엇을 만드는가**(서비스 정의·기능 요구사항·구현 프로세스·누락 검토 시나리오)는 `PRD.md` 2.0.0에 있다.
- **앞으로 할 일의 순서**는 `PRD.md` 13장 "과제 나래비"가 정본이다. 프로세스 순서로 정렬돼 있다.
- 현재 상태·실측 수치는 `HANDOFF.md`, 재발견 금지 사실은 `CLAUDE.md`.

아래 P0~P8은 2026-08-07에 잡은 단계 구분이고, 완료분의 근거와 측정치를 담는다. 남은 항목은 PRD 13장으로 넘어갔다.

## 왜 이 작업을 하는가

파이프라인은 전 구간 관통하고 재생·커서·스템 믹서까지 동작한다. 문제는 **나오는 악보를 실제 연습에 쓸 수 없다**는 것이다.

1. **표기가 너무 촘촘하다.** 16분 격자 고정(`quantize.py` `DEFAULT_SUBDIVISION = 4`) 때문에 16분 299 / 8분 242로 적힌다. 같은 곡 참조 악보는 8분 중심이다.
2. **원곡 난이도 그대로다.** 초보가 읽을 수 있는 형태로 낮추는 수단이 없다.
3. **악보 구성이 참조 악보와 다르다.** 참조는 3단(보컬 멜로디+가사 / 베이스 오선 / 베이스 TAB)에 코드 심볼이 붙는데 우리는 베이스 오선+TAB 2단이고 코드도 가사도 없다.
4. **키 조절이 어긋난 상태다.** 피치 조절 UI는 살아 있는데(`TransportBar.tsx` → `PlayerShell.tsx` → `player.setSemitones`) 악보가 이조되지 않는다. 키를 올리면 들리는 음과 악보가 다르다.

**목표는 결과물 품질을 상용 수준으로 올리는 것.** 외부 서비스 공개·배포·인증·저작권 대응은 범위 밖이고 PRD 2.2 비목표를 유지한다.

**완성 판정은 골든셋 정량 지표.** 참조 악보 2종은 개발 중 타깃으로 쓰고, 릴리스 게이트는 골든셋이 잡는다.

---

## 목표 악보 — 참조 자료 판정

자료 위치: `C:\Users\admin\Desktop\악보\` (akbobada 4현 편곡판, 저작권 자료라 저장소에 넣지 않는다)
- `예뻤어\` PNG 3장 — DAY6, 2단(보컬 오선+가사 / 베이스 TAB)
- `드라우닝\` PNG 5장 — WOODZ, **3단(보컬 오선+가사 / 베이스 오선 / 베이스 TAB)** ← **채택된 목표 형태**
- `악보보기 _ 악보바다.pdf` — Champagne Supernova, 근음 검증용

| | 예뻤어 (DAY6) | Drowning (WOODZ) |
|---|---|---|
| 템포·조성 | ♩=84, E♭, 4/4 | ♩=111, A♭, 4/4 |
| 최소 음길이 | 8분 (일부 16분) | **8분 고정** |
| 마디당 음 수 | 4~8, 쉼표·스타카토 있음 | **8 고정, 쉼표 사실상 0** |
| 음높이 | 마디당 근음 1개 | 마디당 근음 1개 |
| 프렛 | 1~10 (주로 1·3·4·5·6) | 1·4·6·7·8·9 |
| 기법 | 슬라이드·뮤트(×) 소량 | 슬라이드(S·글리산도) 다수, Over Drive 지시 |
| 오른손 부하 | 초당 2.8타 | 초당 3.7타 |

**둘 다 난이도 Lv2(초급)**이고 드라우닝이 그 안에서 위다(템포 32% 빠르고 쉼표가 없어 스태미나 부담). 표지에 "4현 베이스에서 연주할 수 있도록 제작되었습니다"라고 명시돼 있다. 참조 PDF(Champagne Supernova)도 같은 계열 — **akbobada 초급판의 공통 문법은 "근음 + 균일 8분"이다.**

참조 악보는 A♭를 **이조 없이 E현 4프렛**으로 풀었다. 저프렛으로 못 내려가는 조성을 이조가 아니라 옥타브 선택으로 해결하는 방식이고, 우리 기본 동작도 이걸 따른다.

---

## 확정된 원칙

### 난이도는 하향 단방향

원곡 채보(Lv5)가 상한이다. 상향은 편곡이므로 하지 않는다.

**불변식 — 어떤 레벨에서도 깨지 않는다**

1. **시간축 불변.** 마디 수·박자·템포·마디 위치를 바꾸지 않는다. 원곡 음원과 함께 재생되고 커서가 그 위를 달리므로 시간축이 틀어지면 도구가 죽는다.
2. **근음 불변.** 마디의 근음은 마지막까지 남는다. 하향의 종착점이 근음이다.
3. **다운비트에 음이 있다.** 원곡이 1박을 쉬어도 하향판은 1박에 근음을 놓는다. 단 원곡이 통째로 쉬는 구간은 마디 전체를 쉼표로 남긴다.
4. **4현·저프렛 안에서 연주 가능하다.** 프렛 상한·이동 폭 제약을 못 맞추면 옥타브를 접어 맞춘다.

**깎는 순서 — 레벨이 낮을수록 더 내려간다**

1. 기법 표기 (슬랩·고스트·해머). **슬라이드는 남긴다** — 참조 악보의 슬라이드는 기교가 아니라 코드 전환 시 포지션 이동 안내이고, 초급자에게 오히려 쉬운 이동법이다.
2. 비화성음 (경과음·크로매틱)
3. 리듬 세분 (16분 → 8분 → 4분), 짧은 음은 앞 음 길이로 흡수
4. 화성음 (7도·3도 → 5도·옥타브 → 근음만)
5. 음역 (옥타브 도약 접기, 포지션 1개로)
6. 밀도 (8분 8개 → 4분 4개 → 2분 2개)

**레벨 정의**

| Lv | 이름 | 리듬 | 음높이 | 프렛 |
|---|---|---|---|---|
| 1 | 입문 | 2분·온음표, 마디당 1~2음 | 근음만 | 저프렛 고정 |
| 2 | 초급 | 4·8분 균일 | 근음만 | 1~7 ← **참조 악보 두 종이 여기** |
| 3 | 초중급 | 8분 + 부분 16분 | +5도·옥타브 | 포지션 2개 |
| 4 | 중급 | 16분 상시 | +3도·7도·크로매틱 | 제약 없음 |
| 5 | 원곡 | 채보 결과 그대로 | 그대로 | 그대로 |

**판단 규칙**

- **빼지 말고 근음으로 바꾼다.** 음을 삭제하면 리듬 감각이 끊긴다. 참조 악보가 전부 근음 페달인 이유.
- **균일성이 정확성보다 우선.** 마디 안 리듬은 균일하게 맞춘다.
- **원본 불변.** Lv5를 정본으로 두고 모든 레벨은 요청 시 계산되는 파생물이다.
- **하향판임을 표시한다.** "쉬운 버전" 배지.
- **품질 점수는 레벨별로 낸다.** 레벨을 낮추면 거짓음·리듬 오차가 깎여나가므로 신뢰도가 올라간다. 원곡 채보 품질 하나로 하향판을 대표하면 과소평가다.

**금지**: 템포 낮추기로 난이도 조절(재생 속도가 이미 별개 컨트롤), 마디 삭제·반복 축약(시간축 불변식 위반), 조성 변경으로 난이도 조절(키는 독립 축).

### KEY는 재생 피치와 악보가 항상 함께 움직인다

컨트롤 하나(반음 ±)가 둘을 동시에 바꾼다. 어긋난 상태를 만들 수 없게 한다.

- **기본값은 원곡 키 유지.** 저프렛으로 못 내려가면 이조가 아니라 옥타브 선택으로 해결한다.
- **내리면 4현 하한(개방 E, MIDI 28)을 벗어난다** → 벗어난 음만 옥타브 위로 접는다.
- **올리면 프렛이 올라간다** → 운지를 반드시 재계산한다. 이조와 운지를 분리하지 않는다.
- **개방현이 사라져 실제 난이도가 올라간다** → Lv은 "깎는 정도"로만 정의하고 올라간 난이도는 표시만 한다. 억지로 목표 Lv을 맞추면 결과가 예측 불가능해진다.
- **범위 ±6반음, ±3 이상 경고.** 오디오 피치 시프트 품질이 그 밖에서 무너진다.
- **반음 내림은 튜닝 내리기를 먼저 제안한다** — 운지가 그대로 유지된다. `fretting.TUNING_PRESETS`에 E♭ 튜닝 추가로 해결된다.

### 악보 구성은 참조 악보(Drowning)와 동일

3단: 위=보컬 멜로디 오선(가사·코드 심볼) / 중간=베이스 오선(낮은음자리표) / 아래=베이스 TAB(리듬 기둥). 조표·템포·마디번호 표기 포함. **가사까지 재현한다.**

**반복기호는 쓰지 않고 전곡을 펼쳐 적는다.** 참조 악보는 D.S.·To Coda·볼타로 지면을 줄였지만, 우리는 커서가 악보 위를 달린다. 반복 구간을 재생 위치가 두 번 지나가야 하고 커서가 그걸 따라가야 하는데 동기화가 크게 복잡해진다. 자동 넘김 뷰에서는 지면이 아깝지 않다.

---

## 단계 계획

의존 관계: **P0 → (P1 ∥ P2) → P3 → (P4 ∥ P5 ∥ P6) → P7 → P8**

진행 상태는 아래 체크박스로 갱신한다.

### [x] P0. 산출물 재구성 — 완료 (2026-08-07)

**결과**: 이조·튜닝 변형이 **0ms**에 나온다(채보 480초를 건너뜀). 픽스처 3종 F1 1.000 / 1.000 / 1.000, `mix_swing`은 subdiv=3(셋잇단) 유지. 표기 검토 위반 0, 단위 테스트 실패 0, `next build` 통과.

- `pipeline/compose.py` 신규 — 양자화→이조→운지→AlphaTex를 한 함수로 묶었다. **CLI(`run_pipeline.py`)와 웹(`jobs.py`)과 변형 요청(`/api/scores`)이 전부 이 함수를 쓴다.** 셋잇단을 적을 수 없을 때 격자를 되돌리는 폴백이 세 곳에 복사돼 있었는데, 그게 갈라지면 경로마다 다른 악보가 나온다.
- `bassclean.save_notes` / `load_notes` — `notes.json`이 악보의 원본이다.
- `jobs.STAGES`: `quantize`·`fretting`·`alphatex` 3단계를 **`score` 한 단계로 합쳤다.** 폴백 때문에 경계가 원래 없고, 세 단계 합쳐도 분리(450초)·채보(480초) 옆에서 1초가 안 된다. `JobRunner`의 라벨도 맞췄다.
- `GET /api/scores/{hash}?level=&transpose=&tuning=` (워커) + 같은 경로의 Next 프록시. 응답 헤더로 `x-score-octave-folded`(이조로 옥타브를 접은 음 수)와 `x-score-subdivision-forced`를 함께 넘긴다 — 본문은 alphaTab에 그대로 먹여야 해서 섞을 수 없다.
- **구버전 산출물 호환**: `notes.json`이 없는 기존 4개 디렉토리는 원곡·무이조 요청이면 프록시가 구워둔 `score.alphatex`로 메꾼다(`x-score-source: static`). 이조를 요청하면 **409로 거절한다** — 정적 파일을 주면 요청한 것과 다른 악보를 주는 거짓말이 된다.
- `manifest.schemaVersion` 2, `scoreVariants`(가능한 레벨·이조 범위·튜닝) 추가.
- `ScoreView`가 정적 파일 대신 변형 라우트를 부른다. 플레이어 브라우저 확인 완료(악보 렌더·커서 하이라이트, 콘솔 에러 0).

**남은 것**: `data/` 기존 4개 디렉토리는 `notes.json`이 없어 변형을 못 만든다. 필요해질 때 재실행한다(실곡은 약 15분).

### 원래 계획 (참고)

레벨 × 이조 조합이 생기므로 악보를 한 번 굽고 끝낼 수 없다. 그런데 **지금 실곡 산출물에 `notes.json`이 저장되지 않는다**(재진단마다 채보를 다시 돌린다 — `CLAUDE.md` 함정 절). 원본을 저장하고 파생물을 요청 시 계산하는 구조로 바꾼다.

- `apps/worker/jobs.py`: `bassclean` 결과를 `data/{hash}/notes.json`으로 저장. `beats.json`은 이미 캐시로 존재.
- `apps/worker/main.py`: 악보 생성 엔드포인트 신설 — `level`·`transpose`·`tuning`을 받아 `notes.json` + `beats.json`에서 quantize→reduce→transpose→fretting→alphatex를 다시 돌려 AlphaTex를 돌려준다. 재채보가 없으므로 빠르다.
- `apps/web/app/api/artifacts/[hash]/[...file]/route.ts`는 정적 파일 서빙 전용이므로 그대로 두고, 악보용 프록시 라우트를 따로 만든다.
- `manifest.json`에 `scoreVariants`(생성 가능한 레벨·이조 범위), `schemaVersion` 올림.
- **PIP-10(부분 재실행)의 실현**이기도 하다 — 튜닝 변경 시 운지부터 다시 도는 요구사항이 이 구조로 충족된다.

### [x] P1. AlphaTex 문법 실측 — 완료 (2026-08-07)

`tools/probe_notation.mjs` 신규(파싱 + 모델 적용을 함께 확인). 확정 문법은 `CLAUDE.md` 표에 옮겼다.

| 요소 | 문법 | 결과 |
|---|---|---|
| 조표 | `\ks Ab` | **된다** — `masterBars[0].keySignature` raw −4, Major. 참조 악보 A♭(♭4개)와 일치 |
| 멀티트랙 | `\track` 반복 | **된다** — 2트랙 |
| 트랙별 staff·음자리표 | `\staff{score}`+`\clef treble` / `\staff{score tabs}`+`\clef bass` | **된다** — 보컬은 오선만(G2), 베이스는 오선+TAB(F4). **3단 악보 골격이 그대로 선다** |
| 가사 | `\lyrics "미 치 도 록 ..."` | **된다** — 공백으로 나뉜 음절이 비트에 순서대로 배정된다. **한국어도 그대로 붙는다** |
| 코드 심볼 | `4.4.8{ch "Ab"}` | **된다** — 비트에 코드가 붙는다 |
| 슬라이드 | `4.4{sl}.8` / `{ss}` | **된다** — Legato(글리산도) / Shift(참조 악보의 S 표기) |
| 해머·스타카토 | `{h}` / `{st}` | **된다** |

**함정 — `{}`는 위치에 따라 뜻이 다르다.** 음길이 뒤면 duration 수식(`{d}`·`{tu 3}`), 현 번호 뒤면 음표 효과. 효과를 음길이 뒤에 쓰면(`4.4.8{sl}`) 파싱이 실패한다. 효과 이름 7종을 전부 실패로 판정했다가 위치가 원인임을 찾았다.

**가사 비용이 예상보다 훨씬 낮다.** "음절을 어느 음에 붙일지"를 alphaTab이 처리해준다. 남는 문제는 ASR로 음절 열을 얻는 것뿐이고, **표기·정렬은 공짜다.** P5에서 가사를 최후미로 미룰 이유가 약해졌다.

### 원래 계획 (참고)

`\clef`가 **파싱은 통과하고 조용히 무시된** 전례가 있다. 추측으로 넘기면 나중에 전부 되돈다.

확인 대상: 조표(key signature) / 멀티트랙 + **트랙별로 다른 staff 프로필**(보컬=score, 베이스=score+tabs) / 코드 심볼 / 가사 표기와 음절 정렬 / 슬라이드.

도구: `tools/validate_alphatex.mjs`(파싱), `tools/probe_clef.mjs` 방식으로 확장한 모델 프로브(적용 여부). **인라인 heredoc으로 만들면 순정 케이스도 FAIL한다 — 파일로 써서 검증기에 넘긴다.** `apps/web`에서 실행해야 모듈이 해석된다.

산출: `CLAUDE.md`의 "확정된 AlphaTex 문법" 표 갱신. 안 되는 항목은 대체 경로를 여기서 정한다.

### [~] P2. 리듬 해상도 — 측정 완료, 구현 남음 (2026-08-07)

`eval/eval_grid_resolution.py` 신규. **IDMT에 정답이 두 겹으로 들어 있었다** — `misc/music_xml/`(사람이 적은 정답 악보, 음표마다 리듬 값)과 `misc/beats_csv/`(`시각,마디.박` 형식 정답 비트). 템포·위상을 추정할 필요가 없다.

**결론: 고정 8분도 틀리고 고정 16분도 틀리다. 적응적 선택이 맞다.**

두 독립 측정이 일치한다.

| 측정 | 결과 |
|---|---|
| 정답 악보 리듬 값 (1004음) | 16분음표 이상 **19.5%**. 16분을 쓰는 곡 **9/17** |
| 정답 비트 위 온셋 잔차 (928음) | 8분 격자 잔차 중앙값 0.070, 16분 0.000. 16분 필요 비율 **12.8%** |

**곡별로 완전히 갈린다.** 정답 악보에 16분이 없는 8곡은 16분 필요 비율이 **0~2.4%**, 16분을 쓰는 9곡은 **9.1~40.0%**. 두 군이 겹치지 않는다 → **임계값 5%가 17/17을 정확히 가른다.** 이것이 격자 선택 규칙의 근거다.

**실곡(975e)은 격자 해상도를 판정할 소스가 아니다.** 16분 필요 36.9%가 나오는데, 위상을 16분 단위로 8번 밀어봐도 8분 자리에 걸리는 온셋이 44.7~48.3%를 넘지 못한다(정상 곡은 90% 이상). 위상 문제가 아니라 **온셋 자체가 격자에 정렬돼 있지 않다** — 베이스가 둘 섞인 소스라는 기존 진단과 일치한다. 판정은 IDMT로 한다.

**틀렸던 측정 두 번 (같은 실수 방지)**
1. 16분 격자 잔차로 위상을 구했다 → **16분 격자는 16분만큼 밀어도 잔차가 같아서 위상이 4가지 중 임의로 정해진다.** "온셋이 박 위에 하나도 없다"는 불가능한 결과(002: 슬롯분포 0.0/95.8/4.2/0.0)로 드러났다.
2. 허용 잔차 안에서 가장 거친 격자를 골랐다 → 17곡 전부 잔차가 임계값 0.15에 붙었다(0.125~0.149). **음악이 아니라 임계값이 격자를 정하고 있었다.** 003·007은 16분 잔차(0.064·0.002)가 8분보다 훨씬 좋은데 "8분으로 충분"으로 판정됐다.

**구현 완료 (2026-08-07)**

- `quantize.choose_subdivision()` 신규 — 5% 규칙으로 2 또는 4를 고른다. 스윙 판정이 먼저 오고(셋잇단 격자에는 8분/16분 구분이 없다), 온셋이 12개 미만이면 통계가 성립하지 않아 `DEFAULT_SUBDIVISION`을 쓴다. **비트 위상을 여기서 추정하지 않는다** — 그리드의 비트를 그대로 쓴다.
- `QuantizedScore.sixteenth_ratio` 추가 — 격자를 왜 그렇게 골랐는지 남긴다. 이 값 없이 subdivision만 보면 판정 근거를 되짚을 수 없다.
- `alphatex._DURATION_TABLES`에 subdivision=2 표 추가(8슬롯/마디).

| 픽스처 | subdivision | F1 | 품질 |
|---|---|---|---|
| bass_only | 4 → **2** (16분요구 0.0%) | 1.000 | 84 → **88** |
| mix | 4 → **2** (16분요구 0.0%) | 1.000 | 85 → **89** |
| mix_swing | **3 유지** (스윙) | 1.000 | 82 |

**부수 발견 — 표기 검토 도구가 스윙 악보를 한 번도 검산한 적이 없다.** `tools/diag/review_score.py`가 `SUB = 4`를 하드코딩하고 있었고, 정규식의 음길이 대안이 짧은 것부터 늘어서 있어 `8{tu 3}`에서 `8`만 먹었다. subdivision이 4일 때는 `8`이 유효한 토큰이라 조용히 통과했다. 둘 다 고쳤다(manifest에서 격자를 읽고, 대안을 긴 것부터). 이제 4종(8분·16분·셋잇단 격자) 전부 위반 0.

### 원래 계획 (참고)

**측정 먼저.** 온셋이 8분 격자(subdivision=2)에 얼마나 들어맞는지 대 16분이 실제로 필요한 비율을 `data/_datasets/idmt_single`(노트 단위 정답 보유)과 실곡에서 잰다.

- `quantize.py`: 고정 `DEFAULT_SUBDIVISION`을 **온셋을 설명하는 가장 거친 격자 선택**으로 교체.
- `alphatex.py` `_DURATION_TABLES`에 **subdivision=2 표 추가**(8슬롯/마디: 온음표 8, 붙임점2분 6, 2분 4, 붙임점4분 3, 4분 2, 8분 1). `align` 규칙은 subdivision=4 표와 같은 원리.
- **스윙 경로(subdivision=3)를 깨지 않는다.** `mix_swing` 픽스처로 확인.
- `SWING_MAX_BPM_VARIANCE` 게이트와 순서가 얽힌다 — 격자 선택이 스윙 판정보다 먼저 오면 순서를 정리한다.
- 지표: 온셋 F1만 보면 격자 변경 효과가 안 보인다. **"정답 온셋이 우리 격자 슬롯에 정확히 얹혔는가"**를 지표로 만든다.

### [x] P3. 하향 엔진 — 완료 (2026-08-07)

`pipeline/reduce.py` 신규. `QuantizedScore → QuantizedScore` 변환이라 앞뒤 단계를 건드리지 않는다.

**도수는 근음만 알면 구할 수 있다.** 화성 분석 없이 근음 대비 반음 거리로 도수를 판정한다(유니슨·옥타브 0, 3도 3~4, 5도 7, 7도 10~11). 그래서 "7도·3도 → 5도·옥타브 → 근음만" 순서가 코드 인식 없이 구현됐다.

**리듬 템플릿은 원곡 마디의 음 밀도에 맞춰 고른다.** 음이 두 개뿐인 마디에 8분 여덟 개를 넣으면 안 친 것을 적는 것이다. 밀도를 2의 거듭제곱으로 떨어뜨려 8/4/2/1음 템플릿 중에서 고른다.

**음을 지우지 않고 근음으로 대체한다.** 그래서 하향해도 음 수가 줄지 않는 레벨이 있다(Lv3~5). 리듬 감각을 끊지 않으려는 것이고, 실제로 단순해졌는지는 **쓰인 피치 종류**로 봐야 한다.

| Lv | 이름 | 음 수 | subdiv | 최대프렛 | 쓰인 피치 종류 |
|---|---|---|---|---|---|
| 1 | 입문 | 156 | 2 | 4 (상한 5) | 9 |
| 2 | 초급 | 348 | 2 | 4 (상한 7) | 9 |
| 3 | 초중급 | 449 | 4 | 4 (상한 12) | 9 |
| 4 | 중급 | 449 | 4 | 4 | 11 |
| 5 | 원곡 | 449 | 4 | 4 | 13 |

`fretting.assign()`에 `max_fret`·`max_move` 하드 제약을 추가했다. **비용 가중치로 저포지션을 "선호"하는 것만으로는 부족하다** — 다른 선택지가 없으면 큰 이동이 통과한다. 제약을 만족하는 후보가 없으면 옥타브를 접어 들여놓고, 그래도 없으면 그 음에서만 제약을 풀어 악보에 구멍을 내지 않는다. 접은 피치를 `FrettedNote.pitch`에도 반영해야 한다 — 원래 피치를 남기면 적힌 프렛으로 그 음이 나지 않는다.

`tools/diag/test_reduce.py`로 불변식 4개를 고정했다. 실곡·픽스처 모두 **실패 0건**: 시간축 불변(103마디 시각 동일), 근음 피치클래스 유지(78마디 비교, 불일치 0), 다운비트에 음 있음(위반 0), 운지 제약(초과 0).

**남은 것 — 레벨별 품질 점수.** `quality.py`는 여전히 원곡 기준 하나만 낸다. 하향판의 신뢰도를 정직하게 매기려면 근음 정확도 기준이 필요하고 그것은 P7 골든셋에 달려 있다. 지금은 감산 통계(`ReduceReport`)만 노출한다.

### 원래 계획 (참고)

`apps/worker/pipeline/reduce.py` 신규. `QuantizedScore → QuantizedScore` 변환이라 앞뒤 단계를 건드리지 않는다.

- 마디별 근음 추정(지배 피치클래스). **저장소에 근음 계산 도구가 없다** — 세션 스크래치패드에만 있었다. `pipeline/`에 정착시키고 `eval/`이 재사용한다.
- 깎는 순서 6단을 레벨 파라미터로 구현. 불변식 4개를 단위 테스트로 고정.
- `fretting.py`: `assign()`에 **프렛 상한·마디당 이동 폭 상한** 인자 추가. 기존 비용 가중치(`W_MOVE`/`W_POSITION`/`W_OPEN_BONUS`)는 그대로 두고 하드 제약만 얹는다.
- Lv1~2는 **리듬 템플릿**(균일 8분/4분)을 씌운다. 참조 악보가 실제로 이 모습이고 우리 리듬 오차를 우회한다.
- `quality.py`: 레벨별 점수 산출.

이 단계가 **파이프라인 약점을 그대로 지운다** — 거짓음 9.7%·리듬 과세분·슬랩 누락 51.7%가 근음+거친 격자만 남기면 사라진다. 근음 정확도는 이미 78~82%로 측정돼 있다(무작위 8.3% 대비 9배). 즉 제품에서 가장 먼저 신뢰할 수 있는 산출물이 하향판이다.

### [x] P4. KEY 이조 — 완료 (2026-08-07)

- `PlayerShell`이 `semitones` 상태 하나를 재생 엔진과 `ScoreView`에 **동시에** 내린다. 상태가 하나라 어긋난 상태를 만들 수 없다.
- `ScoreControls` 신규 — 난이도 슬라이더(1~5, 최대값이 "원곡"), 키 ±6 제한, ±3 이상 경고, 튜닝 선택.
- `fretting.TUNING_PRESETS`에 `halfStepDown` 추가. 반음 내림은 이조 대신 튜닝을 내리면 **운지가 그대로 유지된다**.
- `ScoreView`가 난이도·이조·튜닝 변경 시 악보만 다시 받아 `api.tex()`로 갈아끼운다. alphaTab 인스턴스를 다시 만들면 커서 배선과 스크롤 위치가 초기화된다.
- 이조로 옥타브를 접은 음 수를 사용자에게 알린다("키를 옮기면서 N개 음이 4현 음역을 벗어나 옥타브를 올려 적었습니다").

브라우저 확인: 난이도 슬라이더를 Lv2로 내리면 악보가 다시 그려지고 "쉬운 버전" 배너가 나오며 마디 폭 균등·행당 4마디가 유지된다. 콘솔 에러 0.

**조표는 P5로 미뤘다.** 조표를 쓰려면 조성을 알아야 하는데 조성 검출을 하지 않는다. 근음만으로 추정해 틀리면 임시표가 엉망이 되어 악보가 오히려 나빠진다. 코드 인식이 들어오면 조성이 훨씬 신뢰성 있게 나온다.

### 원래 계획 (참고)

- `reduce.py`와 같은 층에 이조 변환. 4현 하한 이탈 음은 옥타브 접기, 이후 `fretting.assign()` 재실행.
- `PlayerShell.tsx`: `semitones` 상태를 `ScoreView`까지 내려 악보를 다시 받아오게 배선.
- 조표 반영은 P1 실측 결과에 따른다.
- `TransportBar.tsx`: ±6 제한, ±3 이상 경고, **"악보 이조 / 튜닝 내리기" 선택**.
- `fretting.TUNING_PRESETS`에 E♭ 튜닝 추가.

### [~] P5. 3단 악보 — 코드 심볼 완료, 보컬 렌더·가사 남음 (2026-08-07)

**[x] 코드 심볼 — 완료.** `pipeline/chords.py` 신규.

근음을 이미 아는 것이 이 문제를 크게 줄인다. 일반적인 코드 인식은 24개 후보를 크로마와 매칭하지만, 우리는 베이스 채보에서 마디 근음을 알기 때문에 남는 판단이 "장3도인가 단3도인가"뿐이다. 크로마는 **other 스템**(화성 악기)에서 뽑는다 — 베이스 스템에는 근음만 있어 3도가 없다.

실곡 검증: 근음 진행 **E → D → Dbm → B 4마디 반복**으로 참조 PDF와 일치하고 **C#만 단조**(E 메이저의 vi). 이조도 정확히 따라온다(+2반음 → Gb E Ebm Db). AlphaTex `{ch "..."}`로 붙고 모델에 반영됨(`bar26:e00 bar27:d00 bar28:dbm00 bar29:b00`).

측정으로 정한 두 가지:
- **7도는 표기하지 않는다.** 문턱 0.55로 재보니 삼화음 곡인데 거의 모든 마디에 7이 붙었고(E7·D7·Dbm7·B7) 같은 D 마디가 D7과 DM7로 갈렸다. 크로마의 7도 성분은 배음·인접 화음에서 샌다. 문턱을 올려 맞추는 것은 곡마다 다른 값을 추측하는 것이므로 하지 않았다. 되살리려면 골든셋으로 채점해야 한다.
- **3도는 마디별로 판정하지 않고 같은 근음의 마디를 전곡에서 모아 한 번 판정한다.** 마디 단위로는 같은 B 코드가 B / B7 / 판정불가로 갈렸다. 한 곡에서 같은 근음 위 코드의 장·단이 바뀌는 일은 드물어 음악적으로도 맞다.

`chords.json`으로 저장하고 변형 요청 때 읽어 이조만큼 옮긴다(피치클래스를 숫자로 남겨 이명동음 표기 문제를 피한다).

**[x] 보컬 채보 — 데이터 확보.** `transcribe_crepe.transcribe()`에 `fmin`/`fmax` 인자 추가, 보컬 음역 상수(80~1100Hz) 정의. `tools/diag/regen_vocal.py`로 실곡 채보 완료 — **496음, 음역 MIDI 52~83(E3~B5)**. 베이스 후처리는 걸지 않는다(배음 제거·누출 제거가 전부 베이스 전제다).

**[ ] 남은 것 — 3단 렌더와 가사**
- `alphatex.build()`의 멀티트랙 확장(현재 `\track "Bass"` 하나 고정). 문법은 P1에서 확인됐고, 오선 전용 트랙의 **음이름 표기 문법(옥타브·음길이)은 실측이 더 필요하다** — `c4 d4`가 파싱은 통과했지만 그것이 "C4 음"인지 "C 음 4분음표"인지 확인하지 않았다.
- 가사: **whisper가 설치돼 있지 않다.** 모델 다운로드 + 검증이 필요해 이번 범위에서 제외했다. P1에서 alphaTab이 음절→비트 배정을 해준다는 것이 확인됐으므로, ASR로 음절 열만 얻으면 표기는 공짜다.

### 원래 계획 (참고)

- **코드 심볼**: `pipeline/chords.py` 신규. 마디별 근음은 P3에서 확보되므로 품질(m/7/dim/분수코드)만 추가한다. 크로마 기반 템플릿 매칭. `madmom`은 Windows 함정이 있으므로 의존성 없는 경로를 우선한다.
- **보컬 멜로디**: 보컬 스템은 이미 분리돼 있다. `transcribe_crepe.py`를 보컬 음역으로 재사용한다 — CREPE는 원래 단선율·보컬이 주 대상이라 베이스보다 잘 나올 영역이다. `bassclean`의 베이스 전용 단계를 타지 않게 분기한다.
- **가사**: ASR + 음절 단위 강제정렬. 이 계획에서 가장 비싸고 한국어 음절 정렬이 관문이다. 보컬 오선이 먼저 서야 붙일 자리가 생기므로 P5 안에서도 마지막.
- `alphatex.py`: `build()`를 멀티트랙으로 확장(현재 `\track "Bass"` 하나 고정). 트랙별 staff/clef는 P1 실측대로.
- `ScoreView.tsx`: 트랙 2개 렌더 설정.

### [ ] P6. 노출 형태 두 가지

현재는 한 형태뿐이다 — 4마디/행 연속 악보를 420px 스크롤 박스에서 `ScrollMode.Continuous`로 따라간다.

**[x] 마디 폭 균등화 — 완료 (2026-08-07).** `display.layoutMode = LayoutMode.Parchment` + `scoreLoaded`에서 `defaultSystemsLayout = 4`. **트랙 값이 악보 값보다 우선하므로 양쪽에 다 넣어야 한다** — 악보에만 넣으면 행당 마디 수가 3으로 남는다. `systemsLayout`(행별 마디 수 배열)도 비워야 `defaultSystemsLayout`이 이긴다.

렌더된 SVG의 마디 번호 x좌표로 검증: **행당 4마디, 간격 118/118/118로 완전 균등.** 콘솔 에러 0.

`displayScale`은 설정할 필요가 없었다 — 기본값 1이라 Parchment가 균등하게 배분한다.

**주의 — 눈으로 판단하면 틀린다.** Parchment 적용 후에도 "1마디가 넓다"고 보였는데, 실제 마디 번호 간격은 157/157로 이미 같았다. 커서 하이라이트(노란 영역)가 1마디를 덮고 있어 넓어 보인 것이다. 첫 행이 다른 행보다 약간 좁은 것은 음자리표·조표·박자표가 들어가기 때문이고 악보 관습대로다.

**아래는 원래 진단 (참고).** 기본 Page 레이아웃의 문제였다. alphaTab의 기본 Page 레이아웃은 마디 폭을 **내용량에 비례해** 배분한다. 그래서 음이 있는 마디는 넓고 온쉼표 한 개뿐인 마디는 좁아져, 한 행에서 마디 하나가 화면 대부분을 먹는다(샴페인 슈퍼노바 1마디 대 2~4마디). 참조 악보는 마디 폭이 균등하다.

→ `LayoutMode.Parchment` + `systemsLayout`/`defaultSystemsLayout`(행당 마디 수) + 마디별 `displayScale`(폭 비율)로 **우리가 폭을 직접 정하는 경로가 있다**(`alphaTab.d.ts`에서 확인). `SystemsLayoutMode.UseModelLayout`으로 모델 값을 쓰게 한다. 실제 적용은 프로브로 확인한다 — 옵션이 있다는 것과 적용된다는 것은 다르다.

- **통상 뷰**: 현재 것을 다듬어 확정. **마디 폭 균등화가 여기 포함된다.**
- **자동 넘김 뷰**: 4마디만 크게 띄우고 커서 진행에 따라 창이 이동. 표시 마디 범위 제한 + 커서 연동 옵션을 P1과 같은 방식으로 실측해 경로를 정한다.
- 두 뷰 토글. **컨테이너 폭을 0으로 만들지 않는다** — 폭 0이면 렌더를 건너뛰고 `renderFinished`가 오지 않아 교착이 생긴다(자리를 잡아두고 `opacity`만 조절).
- 커서 CSS(`app/globals.css`의 `.at-cursor-*`)가 두 뷰 모두에서 유지되는지 확인.

### [ ] P7. 골든셋 확대 + 정량 게이트 — 완성 판정

현재 자산은 IDMT 17곡(베이스 단독) + 합성 픽스처 3종 + 참조 PDF. **분리 단계를 포함한 정확도를 아직 못 잰다.**

- 골든셋 10~20곡. 평가는 **근음 일치율**(코드 진행만 있으면 되므로 곡 붙이는 비용이 낮다). 코드 진행 출처는 Hooktheory TheoryTab, 국내곡은 2곳 이상 대조.
- `eval/`에 근음 일치율 도구 정착(P3의 `pipeline/` 근음 로직 재사용). 기존 `eval_practice.py`의 거짓음·누락 지표는 하향판 평가에 그대로 쓴다.
- **레벨별·이조별 회귀 감시**를 회귀 스위트에 넣는다.
- `quality.py`에 **표기 품질 항목 추가.** 격자 정렬을 5배 개선했는데(0.331→0.064초) 점수가 56→55로 내려간 전례가 있다 — 현 지표에 "읽을 수 있는 악보인가"가 없다.
- **입력 진단·경고**: 연습 영상(원곡 반주 + 커버 연주)은 Demucs가 두 베이스를 한 스템으로 합쳐 품질이 무너진다. 감지 신호 후보는 어택 상승시간 편차(실측 26~128ms로 5배 흔들림)와 보컬 스템 잔존율(59.9%). 조용히 엉망인 악보를 내는 것보다 경고가 낫다.

**두 베이스가 섞인 입력의 채보 기준 — 확정 (2026-08-07)**

`tools/diag/diag_two_basses.py`로 세 신호를 재고 **전부 부정됐다.**

| 신호 | 결과 |
|---|---|
| 진폭(볼륨) | **단봉 분포.** 0.634~0.939에 봉우리 하나, 표준편차 0.070. 두 연주가 갈리면 봉우리가 둘이어야 한다 |
| 같은 피치 쌍의 순서 | 쌍 53개, 진폭비 중앙 0.96, 뒤가 더 큼 34% — 한쪽으로 쏠리지 않는다 |
| 진폭 임계 (결정적) | 임계를 올릴수록 **격자 정렬이 나빠진다.** 8분 자리 63.1% → 41.7%, 16분 요구 36.9% → 58.3% |
| 스테레오 위치 | **완전 모노.** side/mid = 0.0006, L−R 상관계수 1.0000 |

이유: CREPE는 프레임당 피치를 하나만 낸다. 두 베이스가 같은 음역에서 겹치면 둘을 분리하지 않고 **하나의 궤적**을 낸다. 노트 단위로 "커버의 음 / 원곡의 음"이 애초에 나뉘어 나오지 않으므로, 진폭이 큰 음은 둘이 겹쳐 합성된 지점일 수 있고 그것은 커버의 타점이 아니다.

→ **기준: 음높이는 신뢰하고 타점은 격자·템플릿에 맡긴다.** 두 연주는 같은 근음을 치므로 음높이는 살아남고 다른 것은 타점뿐이다. 따라서 이런 입력의 정답 출력은 **하향 Lv1~2(근음 + 균일 리듬 템플릿)**이고, 원곡 그대로(Lv4~5)는 품질 게이트에서 잠그거나 낮게 표시한다. 리듬을 검출에서 가져오지 않으면 섞임이 무해해진다.

부수 확인: 실곡은 위상을 16분 단위로 8번 밀어도 8분 자리 온셋이 44.7~48.3%를 넘지 못한다(정상 곡 90%+). **위상 문제가 아니라 온셋 자체가 격자에 정렬돼 있지 않다.** 그래서 이 곡은 격자 해상도 판정에 쓸 수 없다.

### [ ] P8. 부가 기능 (후속)

- **PDF 다운로드**: alphaTab 인쇄 경로로 비교적 싸게 붙는다.
- **자동 넘김 영상 h.264**: 프레임 렌더 + ffmpeg 조립. 훨씬 무겁다.

### 범위 밖

- **모바일 앱(RN)**: 스택을 바꾸지 않는다. alphaTab은 웹 렌더러이고 재생 엔진이 AudioWorklet(signalsmith-stretch) 기반인데 RN에는 AudioWorklet이 없다 — 둘 다 네이티브로 옮기면 사실상 재작성이다. 앱 확장은 현재 웹을 웹뷰로 감싸는 쪽이 현실적이므로 지금 스택 유지가 앱 계획에 불리하지 않다.
- 외부 서비스 공개·인증·배포 인프라·저작권 대응 (PRD 2.2 비목표 유지)
- 5현 베이스, 기타·드럼·건반 채보

---

## 검증

```bash
# 표기 로직을 건드렸으면 이것부터
.venv/Scripts/python.exe tools/diag/review_score.py data/<hash>/score.alphatex
.venv/Scripts/python.exe tools/diag/review_shortnotes.py data/<hash>/score.alphatex
.venv/Scripts/python.exe tools/diag/test_carry.py
.venv/Scripts/python.exe tools/diag/test_quantize_cross.py

# 문법 (apps/web에서 실행해야 모듈 해석됨)
cd apps/web && node ../../tools/validate_alphatex.mjs <file.alphatex>
cd apps/web && node ../../tools/probe_clef.mjs <file.alphatex>

# 회귀 — 픽스처 3종과 정답셋을 둘 다 돌린다. 반대 방향을 가리키면 IDMT를 믿는다.
.venv/Scripts/python.exe eval/run_eval.py data/<hash> data/_fixture/truth.json
.venv/Scripts/python.exe eval/eval_idmt.py --engine crepe
.venv/Scripts/python.exe eval/eval_practice.py

cd apps/web && npm run build
```

**단계별 수용 기준**

| 단계 | 통과 조건 |
|---|---|
| P0 | 같은 hash에서 `level`·`transpose`를 바꿔 요청하면 재채보 없이 다른 AlphaTex가 나온다 |
| P1 | 확인한 문법이 **파싱 + 모델 적용** 둘 다 통과. `CLAUDE.md` 표 갱신됨 |
| P2 | 8분 중심 곡이 8분으로 적힌다. 픽스처 3종 F1 회귀 없음, `mix_swing` 셋잇단 유지 |
| P3 | Lv2 출력이 마디당 근음 1개·균일 리듬. 불변식 4개 단위 테스트 통과 |
| P4 | 피치 ±N에서 악보 조성·운지가 함께 바뀌고 4현 음역 이탈 0 |
| P5 | 3단이 렌더되고 코드 심볼·가사가 참조 악보 자리에 붙는다 |
| P6 | 두 뷰에서 커서·자동 넘김 동작, 콘솔 에러 0 |
| P7 | 골든셋 근음 일치율 기준선 확보, 레벨·이조 회귀 감시 가동 |

**전체 검증** — 참조 악보 두 곡을 유튜브로 넣어 Lv2를 뽑고 첨부 TAB과 대조한다. 단 akbobada는 편곡이므로 **근음·리듬 값 분포 수준까지만** 유효하다. 마디별 어택 개수를 정답으로 쓰면 안 된다(이 프로젝트에서 이미 한 번 틀렸다 — `eval/golden/champagne_bars25_28.json`의 `_WARNING` 참조).

브라우저 검증은 playwright MCP로 한다(Chrome이 실행 중이면 chrome-devtools MCP는 프로필 충돌로 붙지 못한다). 프로덕션 서버는 CSS 변경 후 **재빌드 + 재시작**이 필요하고, 포트 3000을 옛 서버가 잡고 있으면 새 서버가 EADDRINUSE로 조용히 죽는다.

---

## 알려진 리스크

| 리스크 | 상태 |
|---|---|
| 슬랩 검출 F 0.577, 누락 51.7% | 구조적 약점. 하향판에서는 근음만 남아 가려지지만 Lv4~5에서는 남는다. 품질 게이트에 낮게 표시 |
| 분리 단계 포함 정확도 미측정 | P7 골든셋으로 처음 잰다 |
| 연습 영상 입력(베이스 2개) | P7 입력 진단으로 감지·경고 |
| `bpm_variance`를 균일 격자 기준으로 재계산하면 안 됨 | `beats.py`가 원본 검출 비트 기준을 유지한다. 0으로 만들면 `quality.beatStability` 거짓 보고 + 스윙 게이트가 열려 전곡 셋잇단 재발 |
| `public/vendor/`는 생성물 | 직접 수정 금지. `sync-vendor.mjs`가 소스이고 signalsmith-stretch 벤더 버그를 자동 패치한다 |
| 가사 정렬(한국어 음절) | 가장 불확실한 항목. P5 최후미에 두어 앞 단계를 막지 않게 함 |
| CPU 처리 시간 15분/5분곡 | 하향·이조는 재채보가 없으므로 여기 더하지 않는다 |

---

## 속도 개선 실험 — 기각 (2026-08-07)

`eval/eval_hop_octave.py`. 두 질문을 한 번에 쟀다(같은 연산이 필요해서).

**음높이 읽기 간격을 2배로(10ms→20ms): 기각.**

| | 10ms | 20ms |
|---|---|---|
| 처리 시간 | 929초 | **460초 (절반)** |
| 피치 정확 | 85.3% | 75.2% |
| **누락** | 13.7% | **24.2% (+10.4pp)** |
| 거짓 음 | 9.1% | 7.8% |
| 온셋 오차 | 31.9ms | 33.6ms |

처리 시간은 정확히 절반이 된다. 그런데 **음 10%가 사라진다.** HANDOFF에 "온셋 정밀도 10→20ms는 16분음표 200ms 대비 무해할 것"이라 적어둔 추측은 방향이 틀렸다 — **정밀도는 실제로 무해했다**(31.9→33.6ms). 문제는 다른 데 있었다: `MIN_NOTE_SEC = 0.06`이 6프레임에서 3프레임이 되면서 짧은 음이 문턱 아래로 떨어진다. 시간 절반을 위해 음 10%를 버리는 거래는 성립하지 않는다.

**옥타브 오탐: 문제 아님.** 948음 중 **3음(0.3%)**. CREPE가 프레임당 피치를 하나만 내는 모델이고 `fmin`을 32.7Hz까지 내려 E1을 후보에 넣어둔 결과다. 배음으로 옥타브가 튀는 것은 다성 모델(basic-pitch)의 문제다.

**남은 속도 후보와 그 한계**

- 모델 크기: torchcrepe는 `tiny`와 `full`만 제공한다. tiny는 이미 기각됐다(핑거 누락 2배, 슬랩 붕괴). 중간이 없다.
- 갈라내기·박자 병렬: 비트 **위상 피팅이 베이스 스템을 쓴다**(`phase_source`). 완전 병렬이 안 되고, 비트 검출 부분만 겹칠 수 있어 이득이 수십 초다. 구조 변경 비용 대비 낮다.

즉 **음높이 읽기 속도는 지금 구조에서 더 줄일 수 없다.** 줄이려면 정확도를 내줘야 하고 그 거래가 성립하지 않는다. 세션 작업 쪽 절약(무거운 작업 겹치기, 조합 전수 검증으로 왕복 줄이기, 독립 측정 병렬)이 실질적인 개선 경로다.

## 표준 기법 대조 (2026-08-07)

분리·AMT·렌더링의 통상 기준을 우리 코드에 대조했다.

| 기준 | 우리 상태 | 위치 |
|---|---|---|
| 타임도메인 직접 분리 | **반영** — htdemucs | `separate.py` |
| HPSS·NMF | 안 씀 (딥러닝으로 대체) | — |
| 기음 추적, 배음 제거 | **반영** — CREPE는 프레임당 피치 1개라 배음 거짓음을 구조적으로 안 만든다 | `transcribe_crepe.py` |
| 단음/다음 구분 | **반영** — 베이스는 단음이라 CREPE. basic-pitch는 비교용 보유 | `transcribe.py` |
| 퀀타이즈·비트 트래킹 | **반영** — beat_this + 균일 격자 피팅 + 적응 subdivision | `beats.py`, `quantize.py` |
| 그리드 분할 단위 | **반영** — 8분/16분/셋잇단 적응 선택(임계 5%) | `quantize.choose_subdivision` |
| 운지 DP 최적화 | **반영** — Viterbi, 개방현 보너스·저포지션 편향·이동 제약 | `fretting.py` |
| 렌더링 엔진 | **반영** — alphaTab(오선+TAB 병기, Parchment 균등 마디) | `ScoreView.tsx` |
| **음량 임계(Velocity Threshold)** | **오늘 추가** — 그전까지 없었다 | `bassclean.measure_loudness/gate_by_loudness` |
| **어택 에너지(Onset Detection)** | **없음.** 측정 후 미도입 | 근거는 HANDOFF |
| **킥드럼 구별(조화성/충격성)** | **없음.** Demucs에 전적으로 맡기고 검증하지 않는다. `drums` 스템은 채보 로직에서 한 번도 참조되지 않는다 | 갭 |
| ADSR 엔벨로프 판정 | **부분** — 짧은 음 제거만. 어택/서스테인 구별 없음 | `MIN_NOTE_SEC` |
| 정중앙(센터) 활용 | **없음.** 실곡 bass 스템이 side/mid 0.0006으로 완전 모노여서 쓸 정보가 없었다 | 측정 완료 |

**`MIN_AMPLITUDE = 0.25`는 음량 임계가 아니었다.** `Note.amplitude`에 CREPE periodicity(확신도)가 들어가므로 그 상수는 확신도 하한이다. 실제 음량과의 상관계수는 **−0.2024** — 거의 무관하다. 표준 기준의 "음량 임계값"에 해당하는 것이 파이프라인에 **없었고**, 오늘 `loudness`를 별도 필드로 추가해 채웠다.

**어택 검출을 넣지 않은 근거**(HANDOFF 상세): superflux + 16분 그리드 스냅이 F 0.545인데 "모든 슬롯을 어택으로 찍기"도 F 0.545였다. precision이 우연 수준(0.396 vs 0.375)이라 그 오디오에서 어택 정보를 뽑지 못했다. 다만 이것이 **슬랩 누락 51.7%의 원인**이고, 마디당 타수 부족의 원인이기도 하다 — 다시 볼 가치가 있는 항목이다.

## 결정 로그

| 날짜 | 결정 | 근거 |
|---|---|---|
| 2026-08-07 | 난이도는 하향 단방향만 | 원곡 채보가 상한. 상향은 편곡이라 채보 상한을 넘을 수 없다 |
| 2026-08-07 | 목표 악보 형태 = 드라우닝 3단 구성, 가사 포함 | 사용자 지정 |
| 2026-08-07 | 반복기호 안 쓰고 전곡 펼쳐 적기 | 커서가 반복 구간을 두 번 지나가야 해서 동기화가 복잡해진다 |
| 2026-08-07 | 슬라이드는 하향에서도 남긴다 | 참조 악보의 슬라이드는 기교가 아니라 포지션 이동 안내 |
| 2026-08-07 | Lv은 "깎는 정도"로만 정의, 이조로 오른 난이도는 표시만 | 목표 Lv을 억지로 맞추면 결과가 예측 불가능해진다 |
| 2026-08-07 | 상용 범위 = 결과물 품질만. 외부 공개는 범위 밖 | PRD 2.2 비목표 유지 |
| 2026-08-07 | 완성 판정 = 골든셋 정량 지표 | 참조 악보는 편곡이라 어택 개수를 정답으로 쓸 수 없다 |
| 2026-08-07 | RN 전환하지 않고 웹뷰 래핑 전제 | alphaTab은 웹 렌더러, 재생 엔진은 AudioWorklet 기반. RN 네이티브 이식은 재작성 |


---

## 2026-08-07 후반 — 관성 배선 · 게이트 지표 교체 · 정답 59마디로 확장

### 한 것

1. **패턴 관성을 파이프라인에 배선했다.** `compose.build()`에서 양자화 뒤,
   하향 앞에 들어간다(검출 보정이므로 원본 단계에도 적용된다). 그전에는
   측정 스크립트로만 검증돼 있었고 실제 산출물에는 반영되지 않았다.

2. **게이트 판정 지표를 8분 정렬 → 16분·셋잇단 중 최선으로 바꿨다.**
   정상 곡 오발동 **6/17(35%) → 2/17(12%)**. 근거는 NEXT.md 인사이트 절.
   판정과 감량에 다른 지표를 쓴다(`GATE_TARGET_GRID_RATIO` 대
   `GATE_CONVERGE_TARGET`).

3. **정답을 16마디 → 59마디로 늘렸다.** `eval/eval_video_bars.py`(신규) +
   `eval/golden/champagne_video_bars25_40.json`·`champagne_video_bars41_99.json`.
   유튜브 프레임 15장을 Gemini vision으로 판독하고 Claude가 6개 시스템을
   재판독해 대조했다. **판독이 2:1로 갈린 16마디는 정답에서 뺐다**(영상 73~88).

4. **구조 분할 모듈(`sections.py`)을 만들었다.** 리듬 특징 SSM + 노벨티 +
   4배수 스냅. 경계는 잘 찾지만 결과가 나빠져 **끄고** 판정을 골든셋 확대
   뒤로 미뤘다.

5. `alphatex.slots_of()`가 `4{d ch "E"}` 같은 합친 중괄호를 되읽지 못해
   정답 대조 도구가 통째로 죽던 것을 고쳤다(`_strip_non_duration`).

### 드러난 것

**16마디 100%는 과적합이었다.** 59마디에서 자리 93% / 타현 **63%**.
연주자가 쓸 수 있는 선(타현 90%)에 크게 못 미친다. 실패는 속주 구간과
관성 창 경계 두 곳에 뭉쳐 있다.

### 측정에서 진 시도 — 다시 하지 말 것

| 시도 | 결과 |
|---|---|
| 관성 창을 구조 분할 경계로 자르기 | 타현 100%→88%(16마디), 63%→53%(43마디) |
| 게이트 마디 보호에 16분 격자 조건 추가 | 73%→53%, 평균오차 0.51→0.80 |
| 겹침을 "일정 시간차로 짝지은 온셋"으로 판정 | 80ms에 봉우리는 있으나 전체의 8%, 속주 과다검출과 무상관 |

### 회귀 (전부 통과)

코드 11건 / 하향 불변식 / 표기 이월 / 양자화 교차 / 조합 45개 / 정답 59마디 /
`next build`.


---

## 2026-08-07 밤 — 경쟁 조사 · Songsterr 벤치마크 · 내보내기

### 시장 조사 (`MARKET.md`)

Gemini에 맡기고 **웹으로 대조했다.** Gemini 답변에 사실 오류가 4건 있었으므로
확인된 것만 기록했다(Klangio는 유튜브 링크를 받고 TAB을 낸다, Songsterr AI는
자동채보를 한다 등).

결론: **유튜브 링크 → 베이스 TAB은 이미 상용 3사가 한다.** 우리 입력 경로와 핵심
산출물은 차별점이 아니다. 남은 차별점은 **난이도 자동 하향**뿐이고, 그것은
확인된 범위에서 유일하다. 우리는 "채보 정확도로 이기는 서비스"가 아니라
**"채보된 것을 연습으로 바꿔주는 서비스"**로 서야 한다.

### Songsterr 사람 채보 벤치마크 — 우리 화성 축이 검증됐다

`tools/fetch_songsterr.py`로 사람이 만든 Champagne Supernova 베이스 탭을 받았다
(`eval/golden/songsterr_champagne_bass.json`). 튜닝이 `[43,38,33,28]`로 우리
standard와 같다.

| | 4마디 주기 | MIDI |
|---|---|---|
| 원곡 (사람 채보) | A – G – F# – E | 45, 43, 42, 40 |
| 커버 (우리 영상 정답) | E – D – C# – B | 28, 38, 37, 35 |

**피치클래스 차이가 [5,5,5,5] — 완전4도 일정.** 우리 조성 검출이 낸 E major는
원곡 A major에서 정확히 5반음 아래다. 제3자 자료와 상수 이조 관계로 맞았으므로
**화성·조성 축은 자기검증이 아니라 외부 검증을 통과했다.**

부산물:
- "커버가 단순화했으니 우리 8타가 맞다"는 가설이 **기각됐다.** 원곡 코러스는
  마디당 2타인데 커버 정답은 3타다 — 커버가 더 촘촘하다
- 사람이 표기한 섹션 길이 `[9,8,12,8,8,8,4,4,8,8,8,18,8,26]`. 4의 배수가 11/14로
  `SNAP_TOLERANCE`는 근거가 있지만, **4마디 섹션이 2개 있어 `MIN_SECTION_BARS = 8`이
  삼킨다.** 18·26마디 섹션도 `MAX_SECTION_BARS = 16`이 쪼갠다
- 구간별 타현 밀도(2타~8타, 4배 차이)가 하향 레벨 목표 밀도를 **추측이 아니라
  실측으로** 정할 근거가 됐다

### 내보내기 (최대 갭 해소)

`pipeline/export.py` — MusicXML(현·프렛 포함) · MIDI(의존성 없이 직접 작성).
워커 `/api/exports/{hash}.{fmt}`, 웹 프록시, UI 다운로드 버튼, 검증 13항목.
난이도·이조·튜닝이 그대로 반영된다.

### 함께 고친 조용한 버그 세 개

| 버그 | 증상 |
|---|---|
| `route.ts`의 `ORIGINAL_LEVEL = 5` (워커는 3) | 워커가 꺼졌을 때 정적 폴백이 안 걸려 502 |
| UI가 게이트를 `eighthAfter`로 읽음 (키는 `gridAfter`) | **"베이스가 둘 섞였다" 경고가 아예 안 뜸** |
| `slots_of`가 `4{d ch "E"}`를 못 읽음 | 정답 대조 도구가 통째로 죽음 |

세 개 모두 "값이 없으면 안전한 기본값으로 흐르는" 형태라 테스트가 없으면
드러나지 않는다.

### 회귀 (전부 통과)

내보내기 13항목 / 코드 11건 / 하향 불변식 / 표기 이월 / 양자화 교차 / 조합 45개 /
정답 59마디 / `next build`.


---

## 2026-08-07 심야 — 재타현 분리 시도(기각) · 진단 정정 · 규칙값 적재

### 정확도 시도: 재타현 분리 — 측정에서 졌다

`pipeline/reattack.py` + `eval/eval_reattack.py`. 같은 피치가 이어지는 구간에서
스펙트럼 플럭스(800~6000Hz) 봉우리로 재타현을 찾아 나눈다. 진폭만 보면 컴프레서
펌핑에 속으므로 RMS 재상승을 보조 조건으로 뒀다.

| 설정 | IDMT F | 거짓음 | 재타현회복 | 실곡 반복 47마디 |
|---|---|---|---|---|
| **끄기(기준선)** | **0.879** | **9.8%** | **84.8%** | **40/47** |
| RMS 조건 강함 | 0.878 | 9.9% | 84.8% | 40/47 |
| RMS 허용 0.9 | 0.869 | 12.0% | 85.3% | **0/47** |
| RMS 조건 없음 | 0.858 | 14.1% | 85.3% | 0/47 |

쓸 수 있는 중간값이 없다. `USE_REATTACK = False`로 껐고 반증 표를 모듈 머리말에
남겼다 — 지우면 다음 세션이 같은 가설로 다시 만든다.

**애초에 여지가 작았다**: 같은 피치 재타현 회복률이 이미 84.8%. IDMT 948음 중
같은 피치가 22%이므로 완벽히 고쳐도 상한 3.3pp다.

### 진단 정정 — 내가 없는 문제를 만들었다

"다른 피치 끼어듦 21%"는 `bassclean._bar_of`(근사 마디 귀속)로 분석한 결과였다.
그 함수 독스트링에 **"위상은 정확하지 않아도 된다"**고 적혀 있는데 읽지 않고 썼다.
`quantize`의 정식 귀속으로 재니:

| | `_bar_of` | **quantize** |
|---|---|---|
| 정답 피치클래스 | 79.4% | **100.0%** |
| 어긋남 | 20.6% | **0.0%** |

어긋났다고 본 28음은 전부 마디 위치 0.99(마디 끝)에 있었다 — 다음 마디 첫 음이
앞 마디로 귀속된 것이다.

**결과적으로 진단이 좁혀졌다**: 반복 구간에서 **피치는 100% 맞고 타현 수만 틀린다**
(40/47). 순수한 온셋 검출 문제이고, 플럭스로는 안 됐다. 다음 후보는 드럼 킥
동기화(`playing.json` kickLock: 타격의 80% 이상이 킥과 일치)다 — drums 스템을
분리해 놓고 채보에서 한 번도 참조하지 않는다.

### 규칙·정책값 적재

`tools/audit_constants.py`(AST 전수 조사) + `POLICY.md`(근거 등급).

- 상수 **110개** 발견. 근거 주석 **30개 → 3개**로 줄였다(자명 15 제외)
- 등급: 실측 / 차용 / 자명 / 추측(위험) / 추측(무해)
- **추측(위험) 6건**을 명시하고 각각 재는 법을 적었다:
  `MIN_NOTE_SEC`(슬랩 고스트 노트를 자르고 있을 가능성) ·
  `LEAKAGE_REGISTER_MARGIN`(옥타브 도약을 누출로 오인) ·
  `GATE_CONVERGE_TARGET`(발동 판정만 실측하고 수렴 목표는 옛 값) ·
  `GOOD/REFERENCE_THRESHOLD`(사용자에게 "쓸 수 있다"고 말하는 값인데 감) ·
  basic-pitch 임계 · `MODEL_NAME`(htdemucs 대 대안 미비교)

Gemini와 교차 검토했고 **그 판정 중 틀린 것도 기록했다** — "`MERGE_GAP_SEC`와
재타현 분리가 충돌한다"는 지적은 CREPE 경로에서 병합 단계가 아예 돌지 않으므로
성립하지 않는다(`POLICY.md` 6장).

---

## 2026-08-08 — Q1: "배음 오인" 결함은 허구였다 (계획 v2 첫 항목)

### 측정 (tools/diag/diag_subharmonic.py 신규)

가설: Come Together에서 CREPE가 3배음을 f0로 잡는다(+7반음 상관 0.727).
검증: 검출 피치 f 아래(f/2·f/3)에 기음 에너지가 실재하는지 STFT(n_fft 16384)로
음마다 측정. **배음을 잡았다면 f/3에 진짜 기음이 있어야 한다.**

| 곡 | f/2 혐의 | f/3 혐의 | r3 중앙값 |
|---|---|---|---|
| Come Together | 0.0% | **0.0%** | 0.004 |
| Queen (대조) | 1.0% | 0.0% | 0.000 |
| Champagne (대조) | 0.0% | 0.0% | 0.015 |

**기각.** 검출 피치 아래에 에너지가 없다 = CREPE는 기음을 제대로 잡고 있다.

### 진짜 원인 — 측정 도구의 이조 오판 + 검출 누락

마디별 원자료를 열어보니 D 마디들에 실제로 D가 잡혀 있었다(부속음 A·C# 동반 —
리프의 5도와 슬라이드 조각). "+7 이조"는 그 A 성분이 만든 가짜 상관이었다:
진짜 이조(Queen)는 0반음 대비 상관 차이가 **1.097**, 가짜(CT)는 **0.085**.

수정 두 건 (`eval_songsterr.py`):
1. `MIN_TRANSPOSE_MARGIN = 0.3` — 최적 이조가 0반음을 이만큼 못 이기면 채택 안 함
2. **피치클래스 지표를 항상 보고** — 자리(현·프렛)는 운지 선택이 다르면 같은 음도
   틀리다고 보므로(D2 = A현5 또는 E현10), "맞는 음을 냈는가"와 분리했다.
   오프셋 탐색 기준도 자리→피치클래스로 (운지 차이에 강건).

### 갱신된 골든셋 수치

| 곡 | 피치클래스 | (구 보고) | 타현 |
|---|---|---|---|
| Come Together | **65%** | 28% | 5% |
| Queen AOBTD | 62% | 62% | 4% |
| Virtual Insanity | 22% | 16% | 0% |

### 남은 것은 결함 하나로 좁혀졌다

CT의 진짜 갭은 **검출 누락**: 진실 580타 vs 우리 251타(43%). 피치는 잡히면 맞는다.
이것은 VI의 10배 누락과 같은 축이다 → **Q2에서 함께 본다.**

## 2026-08-08 — Q2: 박자 오검출 수정 + 엔진 자동 폴백 (VI 붕괴의 정체)

### 1. 박자표 3/4 오검출 — 구조 버그 수정

`beats._infer_beats_per_bar`: VI의 다운비트 간 비트 수가 **{4:52, 2:51, 3:9} 쌍봉**
— beat_this가 다운비트를 마디/반마디마다 섞어 찍는다. 이 상태의 중앙값이 두 봉우리
사이 소수값 **3**에 떨어져 박자표가 3/4이 됐다. "2→4 승격" 규칙이 집계 **후**라
무력했다. **정규화(2→4 승격, 1 폐기)를 집계 전으로** 옮겼다. 진짜 3/4 곡은 3이
지배하므로 영향 없음. 수정 후 VI 4/4 정상.

### 2. VI 골든셋 오디오가 잘못이었다 — MV는 축약 편집판

우리 234초(MV) vs Songsterr 130마디(앨범 5:41). 구간이 잘려나간 편집판은 상수
오프셋 정렬이 성립하지 않는다. **앨범 버전(b9Y4TACmvE8)으로 교체**, `data/d4fd7b689b9db1bb`.

### 3. CREPE 붕괴 감지 → 엔진 자동 폴백 (`pipeline/engine_select.py` 신규)

VI에서 CREPE(full)가 특이적으로 붕괴한다 — 스템에 라인이 실재하는데(pyin 유성
36.8%, E♭m 음계 일치) periodicity 중앙값 0.002. 진폭 무관(내부 정규화 실증).
basic-pitch는 같은 스템에서 771음·피치 분포가 사람 채보(836타)와 일치.

판정 지표 = **노트 커버리지/스템 활동**: 예뻤어 0.83 · Champagne 0.80 · Drowning
0.68 · Queen 0.47 · CT 0.44 · **VI 0.27**. 임계 0.35(붕괴와 차하위 사이).
웹(jobs)·CLI(run_pipeline)·재생성 도구 전부 auto가 기본.

### 4. 함께 잡은 경로 갈라짐 2건

- **CLI에 코드 단계가 없었다** — 드라우닝·예뻤어 산출물에 chords.json 부재.
  `chords.detect_and_save()` 공용 함수로 승격, 세 경로 통일.
- jobs.py `gate_by_loudness`에 `beats_per_bar` 미전달 (4/4 아닌 곡에서 마디 경계 어긋남).

### 결과 (VI 앨범, Songsterr 96마디 대조)

| | CREPE(수정 전) | basic-pitch 폴백 |
|---|---|---|
| 악보 음표 | 214 | **619** |
| 피치클래스 | 22% | **25%** |
| 타현 | 0% | **15%** |
| 조성 | — | **E♭m(조표 G♭) trusted — 정답** |

VI는 여전히 최약 케이스(의도된 하드 케이스). 남은 축은 밀집 믹스 분리 품질(X2)과
16분 마디 정렬. 회귀: 양자화 교차·이월·하향 불변식·코드 11건·조합 45개 전부 통과.

### 진 시도 (재시도 금지)

- **위상 소스를 드럼 스템으로**: 정렬 오차 수치는 좋아 보였지만(0.17→0.15) 재조립
  결과 음표 619→392, 타현 15%→3%로 붕괴. 원복함. 온셋 집합이 달라 수치 비교가
  성립하지 않았다 — 겉보기 정렬 개선에 속지 말 것.

## 2026-08-08 — Q3·Q4: 골든셋 6곡 체제 + 일괄 회귀 러너

### 골든셋 확대 (4→6곡)

- **드라우닝**(65ef1cf020561a5c, NbKH4iZqq1Y) + **예뻤어**(8181e1aa7d7a0be1, BS7tz2rAOSA).
  Songsterr 사람 채보 확보(`songsterr_woodz_drowning.json` 113마디,
  `songsterr_day6_ywb.json` 98마디 — 영어커버판과 성적 동일해 원곡판 채택).
- CLI 경로에 코드 단계가 생기면서 두 곡 다 chords.json·key 보유.
  **드라우닝 조성 A♭ major trusted — 참조 악보(♭4)와 일치, `\ks Ab` 발행됨.**
  코드 근음 진행(A♭·C·F·E♭·D♭)도 참조와 일치. 3도 판정만 참조와 다름(C/F ↔ Cm/Fm).
  예뻤어는 E♭ 검출(정답)이나 diatonicRatio 0.58 < 0.60으로 trusted=False — 아깝게
  임계에 걸림. 골든셋이 늘었으니 임계 재측정 후보.

### 일괄 회귀 러너 `eval/run_goldenset.py` (신규)

6곡 × (원곡 | --level N) 표 출력. **Lv2에서 피치클래스 = 근음 정확도**이므로
LVL-09(레벨별 품질)의 측정 경로가 이걸로 선다.

| 곡 | 피치클래스(Lv5) | Lv2 | 타현(Lv5) | 엔진 |
|---|---|---|---|---|
| 예뻤어 | **98%** | 98% | 49% | crepe |
| Champagne | 94% | 95% | 3%* | crepe |
| Drowning | **81%** | 81% | **83%** | crepe |
| Come Together | 65% | 61% | 5% | crepe |
| Queen | 62% | 62% | 4% | crepe |
| Virtual Insanity | 25% | **35%** | 15% | basic-pitch |

*Champagne 타현 3%는 Songsterr 원곡 기준이라 낮게 나옴(우리 오디오는 커버 —
타현 정답은 영상 정답 59마디 쪽이 유효, 그쪽은 63%).

**"하향이 신뢰도를 올린다"(PRD 1.3)가 처음으로 수치 확인** — VI 25→35%.

### kicksync 판정 — 기각 확정

스튜디오 원곡에서도 킥-베이스 락(±30ms)이 Queen 35.9% · VI 18.9% ·
드라우닝 1.6%(우리 최고 성적 곡!) — 문헌 80%와 거리가 크다. 우리 킥 검출
타이밍 체인에서는 전제가 성립하지 않는다. `USE_KICKSYNC` 꺼진 상태 유지,
되살림 후보에서 제외.

## 2026-08-08 — F1: 3단 악보 렌더 (백엔드 완성)

- **프로브 확정**(`tools/probe_vocal_pitch.mjs` 신규): 오선 전용 트랙 표기는
  `<음이름><옥타브>.<음길이>`, `c4` = MIDI 60 (옥타브 = midi//12−1).
- `quantize()`에 `force_phase` 추가 — 보컬을 베이스와 **같은 격자·위상**으로
  양자화한다. 위상이 갈라지면 트랙 간 마디가 밀린다.
- `alphatex.build(vocal=...)` — 보컬 오선 트랙을 위에 얹는다. 타이 대신
  잘라-쉼표(A.10과 같은 거래). 코드 심볼은 베이스 트랙 유지(보컬은 쉼표
  마디가 많은데 쉼표에 코드를 못 붙인다 — N6).
- `compose.build(vocal_notes=...)` + `transcribe_crepe.transcribe_vocal()` 공용
  헬퍼. CLI(`--no-vocal` 옵션)·웹 잡(vocal 스테이지 신설)·변형
  라우트(`build_score_variant`)·재생성 도구 전부 배선.
- **도구 세 개가 멀티트랙을 몰랐다**: `review_score.py`(트랙 분리 + 피치 토큰 +
  `{d ch "E"}` 합쳐진 중괄호 오독 — 붙임점 슬롯이 사라진 것으로 나옴),
  `our_bars()`(보컬 마디가 앞에 끼어 마디 번호 밀림). 수정.
- 검증: 드라우닝·예뻤어 3단 재조립(보컬 459·589음), 파싱 OK, 마디 길이·정렬
  위반 0, 트랙 간 마디 수 일치, **골든셋 6곡 수치 전부 유지(무회귀)**.
- 남은 것: 브라우저 렌더 확인(F4·F7과 함께 playwright 배치), 보컬 조각화
  (비브라토가 반음 경계에서 음을 쪼갬) 병합 폴리싱.

## 2026-08-08 — F2: 가사 (whisper ASR → 음절 → alphaTab) 완성

- `pipeline/lyrics.py` 신규 — faster-whisper(small, int8, CPU) 한국어 ASR →
  단어 타임스탬프 → 한글은 글자=음절로 분해(시간 등분), 영문은 통짜.
- **정렬 원리(프로브 실측)**: alphaTex `\lyrics`는 음절을 음표 비트에만
  순서대로 붙이고 **쉼표는 소비하지 않는다**. 그래서 방출된 보컬 음표 1개당
  음절 토큰 1개를 시간 그리디로 짝짓는다(`ALIGN_TOLERANCE_SEC 0.45`).
  음절 없는 음 = `-`(멜리스마), 음 없는 음절 = 버림(밀면 전체가 밀린다).
- ASR 품질: 예뻤어 첫 소절 "지금 이 말이 우리가 다시 시작하자는 건 아니야"
  **참조 악보와 일치**. 드라우닝 "미치도록"→"지치도록" 1글자 오인(언어확률 1.00).
- 배선: CLI(lyrics 스테이지)·웹 잡(vocal 스테이지에 통합)·변형 라우트·재생성
  도구. `lyrics.json` 캐시로 변형 요청 시 ASR 재실행 없음.
- 검증: 드라우닝·예뻤어 재조립, `\lyrics` 발행·파싱 OK, 모델에 가사 반영 확인.

## 2026-08-08 — Q5 일부: 원곡 경로 음역 접기

`fretting.assign()`에 튜닝 음역 밖 음의 옥타브 접기 추가(13.3-6 갭). 이조
경로에만 있던 접기가 원곡 경로에도 생겨 개방 E 아래 음이 더는 버려지지 않는다.
골든셋 6곡 수치 불변(기존 곡에는 음역 밖 음이 없었음 — 안전망 성격).
잔여 항목(고스트노트 X표 측정·못갖춘마디 위상 채점)은 NEXT.md로 이월.

## 2026-08-08 — X1 파일럿: 자기 채점 지표 검증 (`eval/eval_selfscore.py` 신규)

**정답 악보 없이 녹음 자체를 정답으로 쓰는 되먹임 고리.** 채보와 스템은 같은
타임라인이므로 DTW(synctoolbox) 없이 — 채보를 크로마로 펼쳐 오디오 CQT 크로마와
프레임 코사인 유사도를 잰다. 지표 둘: 일치(agreement, 채보가 있는 곳이 맞는가)와
커버리지(소리 나는 곳을 채보가 덮는가).

| 곡 | 일치 | 커버리지 | 골든셋 피치 |
|---|---|---|---|
| Champagne | 0.913 | 43% | 94% |
| 예뻤어 | 0.880 | 54% | 98% |
| Come Together | 0.843 | 25% | 65% |
| Drowning | 0.792 | 58% | 81% |
| Virtual Insanity | 0.658 | 46% | 25% |
| Queen | 0.642 | **15%** | 62% |

**골든셋 피치클래스와 상관 0.807 — 유효 판정.** Queen 커버리지 15%는 recall
결손이 이 지표에 그대로 드러남을 보여준다(채보된 것은 맞는데 못 덮는다).

이월: quality.py 편입은 점수 가중치 재보정이 걸려 있어 다음 세션
(GOOD/REFERENCE 임계가 현 점수 분포에 캘리브레이션돼 있다 — 성분을 추가하면
임계도 다시 재야 한다). synctoolbox·music21 설치는 진행(조판 정규화용).

## 2026-08-08 — X2 파일럿: 분리 모델 A/B — htdemucs_ft 기각

최약 곡(VI 앨범)에서 htdemucs vs htdemucs_ft(bass SDR 11.6 vs 12.0), basic-pitch
동일 조건:

| | 정리후 음수 | 진실PC상관 | 크로마일치 |
|---|---|---|---|
| htdemucs (현행) | 763 | 0.808 | 0.658 |
| htdemucs_ft | 821 | 0.804 | 0.666 |

**유의미한 차이 없음 + 분리 시간 4배 → 기각.** VI의 25%는 분리가 아니라
검출·마디 정렬 축의 문제라는 방증이기도 하다. 남은 후보: hdemucs_mmi(12.2),
베이스 특화 Roformer(모델 인덱스에 없음 — 등장하면 재평가). audio-separator는
`.venv-sep`에 격리 설치돼 있어 언제든 재실험 가능.

## 2026-08-08 — 엔진 폴백 임계 재측정: CT도 basic-pitch로 (65%→76%)

크로마 자기 채점이 즉시 일을 했다 — Queen 커버리지 15%가 이상해서 CT·Queen을
basic-pitch로 A/B:

| 곡 | CREPE | basic-pitch | 판정 |
|---|---|---|---|
| Come Together | 피치 65% (커버 25%) | **76%** (커버 69%), 마디 89/89 정합 | **교체** |
| Queen | 62% (커버 15%) | 60% (커버 31%) | 유지 (중립·소폭 손해) |

`MIN_COVERAGE` 0.35→**0.45** (개선 최고값 CT 0.44와 손해 최저값 Queen 0.47 사이 —
간격 3pp뿐인 불안정 경계라 머리말에 재측정 조건 명시). CT 디렉토리는
basic-pitch로 재채보 완료, Queen은 CREPE 원복.

갱신된 골든셋 (피치클래스): 예뻤어 98 · Champagne 94 · Drowning 81 ·
**CT 76** · Queen 62 · VI 25. POLICY.md 상수 절도 갱신.

## 2026-08-08 — F4·F5·F6 구현 + 브라우저 E2E (3단 악보 실물 확인)

### 구현 (executor 에이전트, 빌드·타입·린트 통과)

- **A-B 루프**: `StemPlayer.setLoop()` — signalsmith `loopStart/End`가 입력
  타임라인이라 배속 재환산 불필요. 다운비트 스냅(`snapToDownbeat`, 이분 탐색).
  구간 안에서 B를 찍으면 위치를 건드리지 않는다(소리 튐 방지).
- **메트로놈**: lookahead 스케줄러(25ms/0.1s 창), 입력→출력 시각 환산은 기준점
  고정식(`out = base + input/rate`) — 콜백 지터가 클릭 흔들림이 되지 않게.
  루프 되감기를 스케줄러가 선반영. 다운비트 1000Hz 강세/나머지 800Hz.
- **자동 넘김 뷰**: `display.startBar`+`barCount`(공식 API), 창 경계 고정(1·5·9…),
  `playedBeatChanged`로 현재 마디 추적, 전체 뷰에서도 창 번호 유지(전환 시
  연주 중인 마디가 보인다). **인쇄**: `api.print("", {barsPerRow:4, 전곡})`.

### E2E 실측 (playwright, 드라우닝)

- **3단 악보 실물 렌더 확인**: Vocal 오선(♭4=A♭, 4/4, ♩=111) / Bass 오선 /
  TAB + 튜닝 표기·마디번호·커서·행당 4마디 — 참조 악보와 동일 구성.
- **가사 렌더 확인**: "지치도록 사랑했던 지겹도록…" 음절이 보컬 단에 붙음.
- **F4 동작 확인**: 4마디 자동 넘김 토글 시 마디 1~4만 렌더(Parchment에서도
  startBar/barCount 동작 — 에이전트가 미확인으로 남긴 항목 실측 통과).
- 콘솔 에러 0, `next build` 통과.

### 잡은 버그 — alphaTab은 트랙을 지정하지 않으면 첫 트랙만 그린다

3단 도입으로 첫 트랙이 Vocal이 되자 베이스가 통째로 사라졌다(d.ts:699 명시).
`scoreLoaded`에서 전 트랙 `renderTracks()` — scoreLoaded가 renderTracks로도
재발화하므로 트랙 수 비교 가드로 무한 재렌더를 막았다.

### 함정 (재발견 금지)

- **Turbopack 패닉**(자식 프로세스 0xc0000142): `npx next dev --webpack` 폴백으로
  해결 — PRD 리스크 표의 예고가 실제로 발생했다.
- alphaTab은 시스템(행)을 **뷰포트에 들어올 때 지연 렌더**한다. SVG 텍스트로
  전곡을 검사하려면 스크롤 박스(`max-h-[420px] overflow-auto`)를 실제로 내려야 한다.

### 사용자 귀 검증 필요 (자동화 불가)

루프 경계 클릭 노이즈 / 메트로놈-루프 되감기 첫 클릭 / 배속 시 클릭 간격 —
에이전트 체크리스트 11항목 중 소리 관련은 직접 들어야 한다.

## 2026-08-08 — F7 일부: 보컬 조각 병합

CREPE 분절이 반음 반올림 경계에서 지속음을 쪼개(비브라토) 보컬 단이 16분
조각+쉼표 난수처럼 보이던 것. `transcribe_crepe.merge_vocal_fragments()` —
**같은 피치·간격 80ms 이내만** 병합(다른 피치를 붙이면 꾸밈음을 먹는다).
compose의 보컬 경로에서 적용해 기존 산출물도 재채보 없이 혜택.

- 드라우닝 보컬 459→380음(−17%), 예뻤어 589→525(−11%)
- 재조립 후 마디 길이·정렬 위반 0, 트랙 마디 일치, 파싱 에러 0, 골든셋 무회귀
  (Queen은 CREPE 원복 재채보로 62→64% 미세 변동)

F7 잔여(stretchForce 튜닝·SVG 겹침 실측)와 F8은 브라우저 세션 필요 — 이월.

## 2026-08-08 낮 — 에러 수정 + V 판정 + F7 실측 종결

### 에러 수정

- **`npm run dev` Turbopack 패닉**(자식 프로세스 0xc0000142)을 package.json에서
  `next dev --webpack`으로 영구 회피. 콘솔 에러 0 확인.
- 페이지 타이틀 "Create Next App" → "Lowend — 베이스 채보 연습 플레이어",
  곡 페이지는 `<곡 제목> — Lowend`(탭 구분), html lang=ko.

### V — 참조 악보 재현 판정: 드라우닝 통과

Lv1(초급) 마디 근음 = 코드 근음(참조 악보와 일치 확인된 진행) **95%**(93/98),
마디당 8타 균일이 85/98마디 — akbobada 초급판 문법("근음 + 균일 8분") 그대로.
3단(보컬 오선+가사+조표 / 베이스 오선 / TAB) 브라우저 렌더 + "쉬운 버전" 배너까지
전부 실물 확인. **예뻤어는 71%** — 코드 신뢰가 낮은 곡(diatonicRatio 0.58,
outsideBars 29개)이라 코드 쪽 원인 가능성이 크다. Songsterr 피치 98%가 근음
자체의 정확성은 이미 보증 → 코드 3도·outsideBars 개선(이월)과 함께 재측정.

### F7 — 음표 겹침 실측: 문제 없음 판정

SVG 노트헤드 바운딩박스 간격(같은 행 인접 쌍):

| 곡 | 표본 | 겹침(<−0.5px) | 최소 | p10 | 중앙값 |
|---|---|---|---|---|---|
| Drowning (8분 중심) | 60 | **0** | 0 | 8.1px | 13.5px |
| VI (16분 상시 — 최밀집) | 72 | **0** | 0 | 0px | 10.8px |

겹침 없음. 16분 밀집에서 맞닿음(0px)이 있으나 판독 가능 — stretchForce 조정은
행당 4마디 균등과 상충해 보류. **F7 종결.**

### 측정 함정 (재발견 금지)

alphaTab 지연 렌더는 **스크롤 요소가 창 뷰포트에 보일 때만** 시스템을 실체화한다.
페이지 폴드 아래에 있으면 안쪽 스크롤만으로는 SVG가 영영 안 생긴다 —
`scrollIntoView` 먼저, 그다음 내부 스크롤 + scroll 이벤트 디스패치.

## 2026-08-08 낮 2 — 초급판을 참조 악보 문법으로 (사용자 실물 피드백 반영)

사용자 피드백: "초급이 첨부 악보와 다르다 / 간격이 좁고 4분음표 이격이 불균일".
자동 검증(근음 95%)이 못 본 것 — **밀도·옥타브·포지션·지면**이 다 달랐다.

### 고친 것 네 가지

1. **밀도**: 초급 템플릿이 마디별 검출 밀도를 따라 4분/8분이 출렁였다 →
   **곡 지배 밀도로 페달**(활동이 지배 밀도의 절반 이상인 마디). 참조 문법
   그대로. 픽업·인트로 꼬리는 마디별 유지(안 친 구간에 음을 만들지 않는다).
2. **옥타브**: 검출이 옥타브 위로 잡힌 마디(17마디 A♭2)가 혼자 높게 적혔다 →
   같은 피치클래스 근음의 **곡 내 최저 옥타브로 통일**(pc_floor).
3. **포지션**: `W_THIN_STRING = 0.15` 신설 — 근음을 굵은 현(E·A)으로.
   실측: IDMT 77.8→**79.8%**(0.3은 40.7%로 붕괴 — 상한 확인),
   골든셋 자리 드라우닝 63→**69%** · 예뻤어 68→**77%** · CT 31→35 · VI 14→17.
   피치클래스 무회귀.
4. **지면**: 재생 페이지 max-w-2xl→4xl(악보 607→831px), stretchForce 1.25,
   악보 박스 420→560px.

### 불변식 정정 (PRD 12.4 I5)

"음 수 레벨 단조"는 참조 문법과 충돌한다 — 참조 초급판은 검출 누락 자리도
페달로 채우므로 원곡 채보보다 음이 많다(LVL-05와 같은 원리). test_reduce를
"마디당 음 수 ≤ 레벨 상한(+당김음)" + "피치 종류 단조"로 교체. 실패 0 확인.

### 실물 확인

초급 드라우닝 9~12마디 = 베이스 8분 8타 페달 + TAB 4-4-4-4-4-4-4-4(E현 4프렛,
참조와 동일 포지션) + 가사 정렬. 남은 차이: 8마디 픽업이 온음표로 적힘(참조는
쉼표), C 근음 포지션(우리 A현3, 참조 E현8 — 같은 음).

---

## 2026-08-08 오후 — 화성 규칙 확정 + 음표 원장 + F8 전면 UI (5시간 자율 블록)

### 화성 정합 (사용자: "화음은 꼭 맞아야")

참조 코드 정답: akbobada PNG 8장을 Gemini vision으로 판독(원문
`eval/golden/akbobada_*_chords_raw.txt`), 드라우닝 진행은 세션 초 눈 판독과 일치.

**사람 편곡 문법 7조를 확정하고 reduce.py 머리말에 정식 등재** (판단 기준):
①곡 지배 밀도 페달 ②반마디 코드 체인지 추종(증거 기준 — 픽업 오염 방지)
③화성 가드(근음은 코드 구성음 — 반음 교정 먼저, 재투표 나중) ④저옥타브 통일
⑤성긴 마디는 검출 위치 보존 ⑥E·A현 ⑦슬래시 코드.

경위(측정이 세 번 정정): 반마디 근음 도입 → 예뻤어 지표 급락은 **마디 끝 픽업이
뒤 절반을 오염** → 증거 기준(HALF_ROOT_MIN_*) → 드라우닝 Cm 마디 B(반음 오차)
잔존 → 화성 가드 → 품질 미상 코드의 구성음이 {근음}으로 좁혀져 재투표가 픽업을
승격 → 장·단 합집합 + 반음 교정 우선.

**드라우닝 초급 근음 = 사람 채보 95%** (잔여 5건 = 아웃트로 필인 + 반마디 해석
차이 — 참조 코드와는 일치 확인). 예뻤어의 Songsterr 대조 하락은 지표 부적합
(반마디 화성 + 마디 최빈 동점 + 신뢰 낮은 채보 — 반마디 정밀 채점은 이월).

불변식 정련: I2 = "원곡 마디에 있던 pc(±1)" (화성 가드 교정 인정),
I3 = 페달 마디(4음+)에만 (성긴 마디는 쉼표+픽업이 정답). 3곡 실패 0.
골든셋 6곡 무회귀 (드라우닝 81/69/83 유지).

### 음표 배치 원장 (사용자: "어떻게 들어가는지 다 데이터로")

`pipeline/ledger.py` — 음표마다 [마디·슬롯·박 위치("1박정박"·"+1/2") /
검출 시각→스냅 시각(snap_ms) / 피치 출처(검출·템플릿) / 현·프렛 / 잔차 /
음량·확신도]를 CSV로. `quantize.QuantizedNote.src_start` 필드 신설(출처 추적).
`/api/scores/{hash}/ledger.csv`(+웹 프록시) — 화면과 같은 변형. UI에
"음표 원장 CSV" 버튼. 검증: 초급 마디 9~10이 1박정박·+1/2·2박정박…으로 정확히
박 좌표에 앉음을 데이터로 확인(요약: on_beat_ratio·median_snap_ms 22ms).

### 커서 등속 (사용자: "인디케이터 등속이어야")

격자·싱크는 이미 균일(마디 2.162s·싱크 112개). 원인은 **열 간격** — 보컬
조각이 홀수 16분 슬롯에 앉아 시스템 열을 쪼개면 커서의 픽셀 속도가 출렁인다.
`_snap_vocal_to_eighths`(보컬 슬롯 8분 정렬) + stretchForce 1.25 + 페이지 폭
확장으로 해소. 실측: 재생 중 커서 이동 확인(567→960px), 콘솔 에러 0.

### F8 전면 UI (에이전트 구현, 실측 검증)

악보가 주인공(첫 화면 상단), 하단 고정 트랜스포트 바(재생·속도·피치·A-B·
메트로놈), 설정·믹서는 접이식(요약줄에 현재 값 배지), 앰버 = "지금 켜져 있음",
`components/ui.ts` 표현 모듈. 빌드·타입·린트 통과, 배선 무손상(커서 이동 실측),
스크린샷 확인. 8마디 픽업이 쉼표+8분으로 적히는 것도 실물 확인.

### Gemini 연동 판단 (13.5 원칙 유지)

- **붙임**: 가사 오인식 교정(`lyrics.refine_with_gemini`) — 음절 개수·시각
  불변 강제(어긋나면 통째 폐기), lyrics.json 캐시로 재현성, 참조 가사와 눈
  대조 가능. + 참조 악보 판독(이미 사용).
- **안 붙임**: 하향 편곡·표기 생성·코드 판정 — 채점 불가.

## 2026-08-08 저녁 — 초급 운지를 참조 포지션에 완전 일치 (97dab04, 사용자 지시)

사용자: "C도 E현 8로 맞춰. 참조랑 같이" — 같은 음이라도 참조 악보의 자리를
그대로 재현해야 한다(A현 3 ≠ 참조).

- **초급 전용 운지 가중치**: `w_move 0.15 / w_string_change 3.0 / w_thin_string 0.1`.
  참조 문법 = "직전 현을 유지할 수 있으면 유지한다"를 비용으로 강제.
  `max_move` 5→7 (참조 실측: Cm E현8 → Fm E현1, 같은 현 7프렛 시프트).
- **결과**: C = E현 8 ✓ (마디 11·12·19·20), B♭ = A현 1 ✓ — 참조 2페이지
  TAB "1-1-1-1"과 동일(이전에 E현 6으로 기억했던 것은 착오, A현 1이 참조 정답).
  A♭ E4·F E1·E♭ A6·D♭ A4·D♭m/E E0 전부 참조 일치.
- **반마디 가드 확장**: 반음 이웃 조건 제거 — 뒤 절반 근음이 코드 비구성음이고
  조성 비다이어토닉이면 관성 유래 오검출로 보고 앞 근음으로 통일.
  드라우닝 18마디 B(E7) 제거(남은 A6은 A♭의 5도 색채음 = 문법 내),
  예뻤어 A♭dim7 반마디는 다이어토닉이라 보존(48% 지표 불변으로 확인).
- **검증**: 드라우닝 초급 근음 = Songsterr 95% 유지 / 불변식·조합 0건 실패 /
  골든셋 6곡 무회귀(81·98·94·77·64·25) / 산출물 재조립·워커 재기동·API 확인
  (`?level=1` 응답에 `8.4` 토큰 실림).

## 2026-08-08 밤 — 참조 전 마디 대조(4건 교정) + 악보 연주 모드 + 경쟁 조사

1) **드라우닝 초급 = 참조 TAB 전 112마디 대조 완료** (b25fe7c). 참조 운지의
원리 = "손이 있는 자리 근처": D♭(A4)→/E(A7)→B♭m(A1). 교정 4건 —
반마디 뒤 근음 옥타브는 앞 근음 최근접(_octave_near, /E=E2), 가드 대칭화
(C7 마디 앞 B), 선행 가드(Fm 마디 뒤 E♭, 다음 마디 근음과 같고 비구성음일
때만), 순수 근음 페달(마디 중간 5도·앵커 피치·아래 옥타브 색채 접기).
잔여 어긋남 2곳은 검출 공백(52마디 /E 증거 없음, 83마디 검출 2음)이라
규칙이 아니라 검출 과제. Songsterr 근음 96%, 골든셋 무회귀.

2) **악보 연주 모드** (e69dc7d). sonivox.sf2→단음 41샘플, /synth-notes
엔드포인트, 메트로놈 시계 공유 샘플러 스케줄러, 스템 뮤트 연동 토글+부스트.
E2E 실측 통과. 다음 고도화 후보: 카운트인, 근음 외 색채 재생 검증, AceStudio
류 사실적 음원.

3) **경쟁 6종 조사** → MARKET.md 2026-08-08 저녁 절. 차별점(베이스 특화·
하향·스템 동기·3단 악보) 전부 유지 확인. 도입 1순위 = 인앱 채보 보정 에디터,
2순위 = SNS 링크 입력(yt-dlp 저비용). 실물 정합성 대조(Klangio 20초·
Songscription 30초 무료)는 업로드에 사람 손 필요 — 사용자와 함께 할 항목.

## 2026-08-08 심야 — 보컬 정리 + 보정 에디터 + Klangio 실물 대조

1) 보컬/가사 4건 수정(9b35cfd): ♯난장=스펠링 버그(_uses_flats), 인트로 유령
(_drop_vocal_ghosts), 얼룩 리듬(_legato_vocal), 가사 "미치도록"(artist 힌트+
데이터 교정). 검출 자체는 99% 다이어토닉이었다 — 문제는 표기였다.
2) 인앱 보정 에디터(fde4037): srcStart 오버레이(edits.json) → 전 난이도 전파.
E2E 통과. 남은 것: 템플릿 음 직접 보정, 음 추가, 가사 보정.
3) Klangio 실물 대조(fbafd84, eval/bench/klangio/): 운지·리듬 우리 우위,
코드 장·단 판정은 Klangio 우위(→ 이월 과제 승격 근거), "편곡(난이도 조절)"
모드 존재. Songscription은 로그인 블로커 — 사용자 계정 필요.
4) 악보 스캐너/PlayScore 2 벤치: 파트별 음색·드래그 루프·끊김 없는 템포가
UX 벤치마크(우리는 스템 뮤트/솔로·A-B 루프·실시간 배속 이미 보유 — 갭은
드래그 루프 UI와 파트별 음색 정도).

## 2026-08-09 — 코드 장·단 판정 + Songscription UI 이식

1) **refine_with_key**(58e7a1f): 조성 프라이어 + 마디별 3도 오버라이드(2.05,
실측 갭). 드라우닝 라벨이 참조와 사실상 동일(Cm·Fm·B♭m + C7 자리 4곳 분리).
합산 크로마의 장조 편향(근음 5차 배음) 규명. 경쟁사 대비 유일 열세 해소.
2) **anydesign 클론 분석**(eval/bench/songscription/design.md) → 배치 문법
2건 이식: 하단 바 [원곡|악보 연주] 세그먼트, 악보 헤더 난이도 칩. 팔레트는
의도적으로 미채택(브랜드 분리).
3) 남은 UI 후보: 드래그 구간 루프, 파트별 음색, 별점 피드백(수집처 생기면).

## 2026-08-09 오후 — 악보 감사 → 에디터 확장 (81032fc)

감사 문서: eval/bench/score-audit-2026-08-09.md. 발견·수정:
①베이스 오선 조표 누락(헤더 \ks는 첫 트랙만 — 3단 전환의 형제 버그, 스태프별
방출) ②f 다이내믹 숨김 ③제목 중복 접기. 에디터: 음 추가(add 액션, 검출과
동급 자격)·가사 보정(PUT /lyrics). 좌표계 함정 2건 문서화 — beats.json
다운비트 ≠ 악보 마디 시각(위상 ~0.5s, ledger.json barStarts가 정본),
alphaTab beatMouseDown은 x 기반(스태프는 포인터 y로 직접 판별).
잔여: 92마디 Cm(수용), 보컬 리듬 성글음(수용), "사" 음절 누락(보컬 음 부족),
예뻤어 정밀 채점(이월). 83마디는 에디터 음 추가로 참조와 같은 C 페달로 복구
(테스트 겸 실개선 — edits.json에 1건 남아 있음).

## 2026-08-09 밤 — 정답 우선 로직 2종 (a364720 이후)

1) 가사 정답 1순위: fetch_full_lyrics(Gemini 전문 회상, UNKNOWN 강제) →
apply_known_lyrics(difflib 정렬, 등길이 replace만 치환, 일치율<0.5 불신).
예뻤어 실증(0.77, 10치환), 드라우닝 UNKNOWN 폴백.
2) 참조 악보 캐시 적재 + 자동 대조: ingest_reference.py → reference.json,
compare_reference.py(근음 DP 정렬) → 드라우닝 근음 96%·운지 97%, 실질 0.
다음: **악보 업로드→매칭 재생** — 업로드 UI + reference→alphaTex 표시 +
DP 매핑으로 인디케이터(반나절 추정). 판독 흔들림 대비 2회 판독 다수결 옵션.

## 2026-08-09 심야 — "내 악보" 모드 완성 (악보 업로드→매칭 재생)

pipeline/reference.py + /reference-tex·/reference(업로드) + ScoreView 토글.
핵심 설계 = **오디오 마디 순서로 펼쳐 그리기**(근음 DP 2패스 매핑) — 커서
장치 무수정. E2E: 드라우닝 109/112, 예뻤어 UI 업로드→3페이지 93/97.
잔여: 내 악보 모드에서 악보 연주(신스)는 자동 채보 음을 씀(참조 음 연주는
후속), 리듬 균등 배치(정밀 리듬 판독은 후속), 판독 현 식별 흔들림(다수결 후속).

## 2026-08-09 밤 2차 — 잔여 전체 + 백로그 완주

1) 키 신고 → 벤더 정상 계측(+7=1.501) → 진범 = 내 악보 미이조 → transpose
전 경로 배선(악보·코드명·신스). 2) 내 악보 신스(source=reference).
3) 드래그 구간 루프(E2E 다운비트 일치). 4) 판독 다수결(votes=2, 코드
베이스 음 심판) + 슬래시 근음 규칙 → 드라우닝 운지 100%·근음 95%,
예뻤어 78/72%(잔여 = 우리 운지 '같은 피치 다른 현' + 2절 채보 — 기록).
5) 주법 13종 지식·판독·렌더(docs/tech/bass-techniques.md, alphaTex 문법
실측: 프렛.현{fx}.길이). 6) 백로그: youtube-ingest 스킬(전역) + 대상 영상
분석(eval/bench/yt-S9wYFUunm_E-analysis.md) → B-3(밴드 악보) 파생.
