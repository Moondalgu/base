# NEXT — 다음에 바로 이어갈 것 (2026-08-08 계획 v2로 갱신)

## 실행 계획 v2 (2026-08-08 승인) — 이 순서가 현재 정본

사용자 결정: **품질 → 제품 순서**, 예뻤어(DAY6) 검증 곡 승격, 끝선 = **참조 악보 재현**
(드라우닝·예뻤어 Lv2 3단 악보(가사 포함) + 자동넘김 뷰 + A-B 루프 + PDF).
벤치마크·외부 자산 도입·Gemini 활용 명시 승인, 도입 채택은 골든셋 채점으로 정당화.

```
B1·B2·B3 (벤치마크: 경쟁사 실물 대조·품질 목표 수치·연습 UX 스터디)
   ↓
Q1 배음→기음(Come Together) → Q2 16비트·박자(VI) → Q3 골든셋 확대(드라우닝·예뻤어)
   → Q4 회귀 게이트·LVL-09 → Q5 잔결함(고스트노트·옥타브 접기·위상 채점)
   ↓
X1 synctoolbox 되먹임 ∥ X2 분리 모델 A/B (BS-RoFormer)
   ↓
F1 3단 렌더 → (F2 가사 ∥ F3 조표) → (F4 자동넘김 ∥ F5 A-B 루프·메트로놈 ∥ F6 PDF)
   ↓
V 최종 검증 (참조 악보 재현 판정)
```

상세는 `~/.claude/plans/stateful-forging-valley.md` (승인본). 완료 기록은 PLAN.md에 적재.

### 진행 현황 (2026-08-08 심야 — 상세·수치는 전부 PLAN.md)

- **완료**: B(벤치마크 리서치) · Q1(배음 가설 기각 — CT 28%→65%, 측정도구 수정) ·
  Q2(박자 3/4 버그 수정 + 엔진 자동 폴백 — VI 앨범 교체·25%/타현15%) ·
  Q3(골든셋 6곡 — 드라우닝 81%/타현83%, 예뻤어 98%) · Q4(run_goldenset.py + Lv2 기준선) ·
  F1(3단 악보) · F2(가사 ASR·정렬) · F3(조표 — 드라우닝 Ab·예뻤어 Eb 발행) ·
  Q5 일부(원곡 경로 음역 접기) · 조성 임계 0.60→0.55(예뻤어 정답 근거)
- **진행 중**: F4·F5·F6(프론트 — 에이전트), X2 파일럿(htdemucs_ft 분리), X1(synctoolbox 설치)

### 이월 항목 (다음 세션 후보)

1. **브라우저 E2E**: 3단 악보 렌더·커서·F4/F5/F6 실측(playwright), F7 조판(SVG 좌표), F8 UX/UI
2. ~~V 최종 검증~~ 드라우닝 통과(Lv1 근음=코드 95%·균일8분). 예뻤어 71%는 코드 판정 이월과 연동
3. Q5 잔여: 고스트노트 X표 측정(IDMT expressionStyle), 못갖춘마디 위상 채점(beats_csv)
4. 코드 3도 판정: 드라우닝 C/F가 참조 악보에선 Cm/Fm — 단조 판정 개선(골든셋 2곡 근거 확보됨)
5. ~~보컬 조각화~~ 완료(08-08 merge_vocal_fragments, −11~17%). F7 잔여 = stretchForce·SVG 겹침 실측
6. VI 잔여: 25%는 여전히 최약(밀집 믹스) — X2 결과에 달림
7. 처리 대기 UX·악보 편집기 (이번 범위 밖 확정)

아래 2026-08-07 정리는 **결함 상세·진 시도·인사이트 참고용**으로 유지한다.

---

`PRD.md`가 무엇을 만드는가를 정하고, `PLAN.md`가 실행 기록을 담고, 이 문서는
**바로 집어서 시작할 수 있는 다음 작업**만 우선순위대로 담는다. 항목을 끝내면
근거와 수치를 `PLAN.md`로 옮기고 여기서 지운다.

배경: 2026-08-07에 PM 관점·연주자 관점으로 전면 검토했고 Gemini로 교차 검증했다.
아래는 그 결과에서 나온 것이다. 판정 근거는 각 항목에 적어둔다.

---

## 골든셋이 즉시 찾아낸 것 (2026-08-07 심야)

**곡 하나로는 안 보이던 문제가 세 곡 붙이자마자 네 개 나왔다.** 이것이 골든셋을
먼저 해야 했던 이유다.

| 곡 | 음(피치클래스) | 타현 | 평균오차 | 음원-악보 이조 |
|---|---|---|---|---|
| Champagne Supernova (기존) | **93%** | **63%** | 0.51 | 0 |
| Queen — Another One Bites the Dust | **62%** | 4% | 2.50 | **+1반음** |
| The Beatles — Come Together | 28% | 3% | 3.50 | +7반음(불확실) |
| Jamiroquai — Virtual Insanity | 16% | 0% | 6.62 | 0 |

### 결함 0 — 대조 도구가 이조를 못 찾아 없는 실패를 만들었다

**처음에 Queen이 자리 5%로 나왔다.** 파이프라인 붕괴로 보였는데 아니었다.
피치클래스 분포 상관을 12개 이조에 대해 재보니 **+1반음에서 0.905, 0반음에서
−0.192**였다. 즉 **유튜브 공식 뮤비 음원이 악보(Em)보다 정확히 반음 높다**(Fm).
우리 검출은 그 음원에 대해 맞았다.

이조를 찾아 보정하니 **5% → 62%**로 올랐다. `eval_songsterr.py`가 이제 이조를
먼저 찾고, 이조가 있으면 자리 대신 피치클래스로 비교한다(이조되면 짚는 자리가
달라지므로).

**이번 세션에서 같은 종류의 실수를 두 번 했다** — `_bar_of`로 없는 문제(21%
끼어듦)를 만들었고, 이조를 안 찾아 없는 실패(Queen 5%)를 만들었다. **측정 도구를
먼저 의심하는 습관이 필요하다.**

### 결함 1 — 16비트 곡에서 검출이 붕괴한다 (Virtual Insanity)

- 234초 곡에서 **88음.** 16비트 펑크는 마디당 8~12타라 800타 이상이어야 한다.
  **10배 누락**
- **박자를 3/4로 잡았다**(정답 4/4). 이것 하나로 모든 마디 귀속이 깨진다
- 마디 116 vs 정답 130, 음이 있는 마디가 37개뿐

피치 상관은 0.692로 이조 차이가 없다 — **피치는 보는데 음을 거의 못 찾는다.**
가장 심각한 결함이고, 우리가 못하는 유형을 일부러 골든셋에 넣은 이유가 이것이다.

### 결함 2 — 배음을 기음으로 잡는 것으로 보인다 (Come Together)

박자 4/4 ✓, 마디 89/89 ✓, BPM 83.3 ✓ — **시간축은 정확한데 음이 틀린다.**
최적 이조가 **+7반음(완전5도)**이고 상관 0.727, 0반음도 0.642로 애매하다.
일정한 이조가 아니라 **일부 음에서 5도 위(3배음)를 잡는** 모습이다.

리켄배커 + 피크 연주는 기음보다 배음이 강하게 녹음되는 전형적인 경우다. CREPE는
단선율이라 배음을 별도 음으로 만들지 않지만, **기음보다 배음이 강하면 그쪽을
f0로 고른다.**

**할 일**: f0 후보의 1/2·1/3 지점 에너지를 확인해 기음을 되찾는다.

### 결함 3 — 스튜디오 원곡을 "연습 영상"으로 오판한다 — **고쳤다**

**세 곡 다 `practiceVideo=True`로 판정했다.** 공식 뮤비·공식 음원인데.

그러면 (a) 하향 단계를 안 만들고 (b) "베이스가 둘 섞여 있습니다"라는 **틀린
경고**를 사용자에게 띄운다.

원인: `diagnose`가 게이트 후 격자 정렬률만 본다. 세 곡의 정렬률이 67~78%로
낮은데 그것은 베이스가 둘이어서가 아니라 **우리 검출이 나빠서**다. "정렬이
나쁘다"에 원인이 둘인데 하나로만 해석하고 있었다. 보조 신호인 어택 편차도
1.39~1.50으로 기존 실곡보다 오히려 높아 **못 가른다.**

**고친 방법 — 판정을 버렸다.** 세 신호가 전부 안 갈렸고 어택 CV는 방향이
반대였다(진짜 연습 영상이 1.096으로 가장 낮다). 원인을 단정하지 않고 관측만
보고한다: "리듬 검출 신뢰도가 낮습니다. 베이스가 둘 섞인 음원이거나,
16비트·슬랩처럼 우리가 약한 연주일 수 있습니다."

`practice_video`는 항상 False로 두고(필드는 호환·복구용으로 남김)
`rhythm_confident`로 판단한다. UI 문구도 단정을 뺐다.

**함께 잡은 거꾸로였던 것**: 리듬을 못 믿을 때 원본만 주고 있었는데, **초급이
남아야 할 단계**다. 초급은 `uniform_rhythm=True`로 검출 리듬을 버리고 균일
템플릿을 씌우므로 리듬 품질과 무관하다. 이제 `[1, 3]`을 준다 — 그러지 않으면
골든셋 4곡 전부에서 하향 기능이 안 열린다(우리 유일한 차별점인데).

### 우선순위가 바뀐다

"타현 수를 올린다"가 아니라 **"곡이 바뀌어도 무너지지 않게 한다"**가 먼저다.
결함 1·2·3은 전부 한 곡만 보던 동안 숨어 있었다.

---

## 지금 실제 정확도 (2026-08-07 측정, 정답 59마디)

**이 숫자가 우선순위를 정한다.** 이전에는 16마디에서 100%였는데 정답을 59마디로
늘리자 과적합이 드러났다.

| 정답 구간 | 자리(현·프렛) | 타현 수 |
|---|---|---|
| 영상 25~40 (16마디, 반복 그루브) | 16/16 100% | 16/16 100% |
| 영상 41~99 (43마디, 속주·전개 포함) | 40/43 93% | **27/43 63%** |

도구: `eval/eval_video_bars.py`, 정답 `eval/golden/champagne_video_bars*.json`.
연주자가 쓸 수 있는 선은 타현 90% 이상이다(교차 검증). **63%는 못 쓴다.**

실패가 두 군데 뭉쳐 있다.

- **속주 구간(우리 49~55)** — 정답 6·7·5·4·4·3·6타를 3·3·3·3·9·3·3으로 낸다.
  게이트가 깎고 관성이 3타로 평탄화한다. **아무것도 안 한 쪽(8·8·8·5·9·3·5)이
  정답에 더 가깝다.**
- **끝부분(우리 95~101)** — 정답 전부 3타인데 4타. 관성 창 경계가 걸린다.

---

## 우선순위

### 0. 마디 단위 검출 30% — 이것이 진짜 병목이다

**2026-08-07 후반에 진단이 바뀌었다.** "속주 구간이 평탄화된다"가 아니라
**"우리는 마디를 검출하지 못하고 반복 구조로 추론한다"**가 문제다.

정답 59마디를 반복 구간(정답 3타, 47마디)과 비반복 구간(필인·속주, 12마디)으로
나눠 재면:

| 설정 | 반복 구간 | 비반복 구간 |
|---|---|---|
| 게이트만 (관성 끔) | 14/47 (30%) | 3/12 (25%) |
| 게이트 + 관성 | **40/47 (85%)** | **3/12 (25%)** |

반복 구간에서 관성 **전** 검출은 이렇게 나온다.

```
정답: 3 3 3 3 3 3 3 3 3 3 3 3 3 3 3 3 3 3 3 3
검출: 2 3 4 2 2 3 4 3 4 3 4 1 5 3 2 2 4 3 3 3   ← 8/20
관성후: 3 3 3 3 3 3 3 3 3 3 3 3 3 3 3 3 3 3 3 3   ← 20/20
```

**전체 73%는 관성이 만든 것이고, 관성은 반복이 있는 곳에서만 작동한다.**
비반복 구간은 관성을 켜도 25%에서 안 움직인다(오차는 1.42→1.75로 나빠진다).
반복이 약한 곡이 들어오면 정확도가 30% 수준으로 무너진다.

#### 오차의 정체 — 두 번 재서 정정했다 (2026-08-07 밤)

**1차 진단이 틀렸다.** 처음에 `bassclean._bar_of`로 마디를 귀속시켜 재고
"음 쪼개짐 1% / 다른 피치 끼어듦 21% / 재타현 누락이 주범"이라고 결론 냈다.
그런데 `_bar_of`는 문서에 **"위상은 정확하지 않아도 된다"**고 적혀 있는 근사
함수다(게이트가 마디를 비우지 않게 하는 용도). 피치 귀속 판정에 쓸 수 없다.

`quantize`의 정식 마디 귀속으로 다시 재면:

| 항목 | `_bar_of` 기준 (1차) | **quantize 기준 (정정)** |
|---|---|---|
| 정답 피치클래스 | 79.4% | **100.0%** |
| 다른 피치 끼어듦 | 20.6% | **0.0%** |

**반복 구간 47마디에서 피치는 전부 맞는다.** 1차 측정에서 "어긋났다"고 본 28음은
전부 마디 위치 0.99(마디 끝)에 있었다 — 다음 마디의 첫 음이 앞 마디로 귀속된
것이고, 실제 파이프라인에서는 그런 일이 없다.

**교훈**: 근사 함수를 분석에 쓰면 없는 문제를 만든다. 그 함수의 독스트링에
"정확하지 않아도 된다"고 적혀 있었는데도 읽지 않고 썼다.

#### 남은 것은 순수하게 타현 수다

피치가 100%이므로 오차는 **온셋 검출(몇 번 뜯었는가)** 하나로 좁혀졌다.
반복 구간 47마디, 정답 141타(47×3), 우리 134타. 마디별로는 40/47(85%)만 맞는다.

##### 시도 1: 재타현 분리 (`pipeline/reattack.py`) — **측정에서 졌다**

같은 피치가 이어지는 구간에서 스펙트럼 플럭스 봉우리로 재타현을 찾아 나누는
모듈을 만들었다. `eval/eval_reattack.py --sweep`:

| 설정 | IDMT F | 거짓음 | 재타현회복 | 실곡 반복 47마디 |
|---|---|---|---|---|
| **끄기(기준선)** | **0.879** | **9.8%** | **84.8%** | **40/47** |
| RMS 조건 강함 | 0.878 | 9.9% | 84.8% | 40/47 |
| RMS 허용 0.9 | 0.869 | 12.0% | 85.3% | **0/47** |
| RMS 조건 없음 | 0.858 | 14.1% | 85.3% | 0/47 |

**쓸 수 있는 중간값이 없다.** 조건을 강하게 두면 분할이 거의 안 일어나고
(IDMT 나눔 0건), 조금만 풀면 즉시 과분할로 붕괴한다. `PEAK_RATIO`를 0.20~0.60
훑어도 꿈쩍하지 않았다 — 묶여 있는 것은 플럭스 문턱이 아니라 RMS 조건이었다.

그리고 **애초에 여지가 작았다**: 같은 피치 재타현 회복률이 이미 84.8%다.
IDMT 948음 중 같은 피치가 211음(22%)이므로 완벽히 고쳐도 상한이 3.3pp다.
`reattack.USE_REATTACK = False`로 껐다(지우지 않았다 — 기록이 필요하다).

##### 시도 2: 드럼 킥 동기화 (`pipeline/kicksync.py`) — **첫 곡에서 발동 안 함**

만들었다. 킥이 있는데 우리 음이 없는 자리에 직전 피치를 물려받아 음을 되살린다.
지우는 일은 하지 않는다(되살리는 쪽이 훨씬 안전하다).

**기존 실곡에서는 발동하지 않는다 — 그것이 맞는 동작이다.**

| | 값 |
|---|---|
| 킥 검출 | 205개 = 마디당 1.99 (상식 범위) |
| 킥 위치 | 1박끝 27.3% + 3박끝 27.8% = **55%가 두 자리에 몰린다** |
| **락 비율(베이스 기준)** | **6.1%** (문헌값 80%) |
| 가장 가까운 킥까지 거리 | 중앙 **449ms** ≈ 8분음표 |
| 드럼 스템 RMS | 베이스의 **1/46** |

킥 검출은 맞다(두 자리에 55% 집중은 무작위가 아니다). 그런데 이 곡은 연습
영상이라 **드럼이 스피커 재생 반주**이고 베이스는 직접 연주다 — 서로 안 맞는다.
`min_lock` 게이트가 잡아 되살린 음 0개, 정확도 변화 0.

**판정은 골든셋으로 미뤘다.** Queen "Another One Bites the Dust"가 킥-베이스가
맞물리는 대표 곡이다.

##### 만드는 과정에서 잡은 자기 오류 두 건

1. **킥 2017개 과검출**(초당 6개). 직접 만든 "플럭스 중앙값 대비 배수" 문턱이
   원인 — 플럭스 대부분이 0에 가까우면 중앙값이 작아 문턱이 무의미해진다.
   `librosa.util.peak_pick`(이동 평균 기준)으로 교체하고 **마디당 킥 수 상식
   범위 검사**(`plausible()`)를 넣었다.
2. **락 비율 방향을 반대로 재고 있었다.** 문헌값은 "베이스 타격의 80%가 킥과
   일치"로 분모가 베이스인데 나는 분모를 킥으로 뒀다. 킥이 베이스보다 많으니
   당연히 낮게 나왔고, 그것을 "안 맞는다"로 읽었다.
3. `onset_strength(fmin=40, fmax=120)`이 **빈 멜 필터 경고**를 낸다(n_mels=128을
   80Hz 폭에 밀어넣는다). 강도 곡선이 무효해진다 — STFT 대역 절단으로 바꿨다.

**주의**: 킥 동기화를 **누출 판정에 쓰면 안 된다.** 이미 한 번 그렇게 틀렸다 —
킥 근처에 음이 2.35배 몰려 있는 것은 누출이 아니라 베이시스트가 킥과 락을
맞추기 때문이다. 타현 판정에만 쓴다.

#### 여기서 이미 진 시도 — 다시 하지 말 것

| 시도 | 결과 |
|---|---|
| 관성 창을 구조 분할 경계로 자르기 | 타현 63% → 53% |
| 게이트 마디 보호에 16분 격자 조건 추가 | 73% → 53%, 오차 0.51 → 0.80 |
| 겹침을 "일정 시간차 짝 온셋"으로 판정 | 80ms 봉우리는 있으나 전체의 8%, 과다검출과 무상관 |
| 커버/반주를 **음량**으로 가르기 | 20% (현행 73%). 속주 구간은 검출 음이 거의 전부 중앙값 위 |
| 커버/반주를 **어택 선명도**로 가르기 | 분포가 단봉(0.02~0.04), 최대 10/59. 안 갈린다 |
| 음 쪼개짐 병합 | 26% → 30%. 쪼개짐은 원인이 아니다 |
| **재타현 분리(스펙트럼 플럭스)** | 쓸 수 있는 중간값 없음. 40/47 유지 아니면 0/47 붕괴 |
| `_bar_of`로 피치 귀속 분석 | 근사 함수라 없는 문제(21% 끼어듦)를 만들었다. 실제 0% |

#### 미결로 남긴 것 — 정답이 맞는지 모른다

이 영상 제목에 "초보, 쉬운버전"이라고 적혀 있다. 화면 TAB은 커버 연주자 본인의
채보이고 원곡 채보가 아니다. 속주 구간 정답이 6·7·5·4·4·3·6인데 게이트 전 검출은
8·8·8·5·9·3·5다 — **우리가 틀린 게 아니라 연주자가 단순화한 것일 수 있다.**

음량으로도 어택 선명도로도 판정하지 못했다(위 표). **귀로 들어야 한다.** 그 전에는
속주 구간 수치를 목표로 삼지 말 것 — 없는 목표를 쫓게 된다. 반복 구간(47마디)은
정답이 신뢰할 만하므로 그쪽 수치로 판단한다.

### 0.5 내보내기 — **끝났다** (2026-08-07)

시장 조사에서 우리 최대 갭으로 나온 항목이다(`MARKET.md`). 경쟁 3사는
PDF·MIDI·MusicXML·GuitarPro를 전부 주는데 우리는 하나도 없었다. 자동 채보는
"보정을 전제로 한 출발점"인데 내보낼 수 없으면 사용자가 틀린 곳을 알아도
할 수 있는 일이 없다.

구현한 것:

- `pipeline/export.py` — MusicXML(현·프렛 포함) · MIDI(의존성 없이 직접 작성)
- 워커 `GET /api/exports/{hash}.{musicxml|mid}` — **난이도·이조·튜닝을 그대로 받는다.**
  화면에서 보는 것과 다른 파일이 내려가면 조용한 실패가 된다
- 웹 프록시 `/api/exports/[hash]/[file]` — 정적 폴백을 **두지 않는다**
- `ScoreControls`에 다운로드 버튼. 현재 레벨·키·튜닝으로 링크가 만들어진다
- 검증 `tools/diag/test_export.py` 13항목 — 마디 길이·현·프렛·MIDI 짝까지 되읽어 대조

GuitarPro(.gp)는 독자 바이너리라 직접 쓰지 않는다. **MusicXML을 Guitar Pro가
읽으므로 경로는 이미 열려 있다.** PDF는 alphaTab 인쇄 경로가 훨씬 싸므로 6번에 둔다.

**같이 고친 낡은 상수 두 개** (둘 다 조용히 틀리는 종류였다):

- `app/api/scores/[hash]/route.ts`의 `ORIGINAL_LEVEL = 5` -> 3. 난이도를 5단계에서
  3단계로 줄일 때 놓쳤다. 워커가 꺼져 있을 때 정적 폴백이 안 걸렸다
- `ScoreControls`·`PlayerShell`이 게이트 결과를 `eighthAfter`로 읽고 있었다.
  manifest 키가 `gridAfter`로 바뀌었으므로 `undefined ?? 1 = 1`이 되어
  **"베이스가 둘 섞였다" 경고가 아예 뜨지 않았다**

### 1. A-B 구간 반복 + 메트로놈 (PLY-06, PLY-08)

**왜 1순위인가** — 없으면 연습 도구가 아니라 뷰어다. 초급자는 곡을 처음부터
끝까지 못 친다. 어려운 4마디를 잡아 느린 템포로 무한 반복하는 것이 연습의
기본 형태다. 배속·스템 믹서·키 조절이 아무리 좋아도 루프가 없으면 그 앞에서
멈춘다. 구현 비용도 낮다(엔진에 구간 정보만 주면 된다).

- 마디 단위 A-B 지정(악보 클릭으로 선택되면 가장 좋다)
- 루프 경계에서 끊김·클릭 없어야 함 (PRD 7.3 PLY-06 수용 기준)
- 메트로놈은 비트 그리드가 이미 있으므로 다운비트 강세만 구분하면 된다

### 2. 처리 대기 UX — 백그라운드 + 완료 알림 + 조기 실패 통보

**왜** — 5분 곡에 약 15분이 걸린다. 웹에서 15분을 기다리게 하면 사용자는
브라우저를 닫는다. 더 나쁜 것은 **15분 뒤의 실패**다. 우리는 "악보가 실패해도
플레이어는 동작한다"로 설계했지만, 즉시 실패와 15분 뒤 실패는 완전히 다른
경험이다.

- 잡을 백그라운드로 돌리고 브라우저를 닫아도 살아 있게 (manifest는 이미 남는다)
- 완료 알림 경로 하나 (브라우저 알림이면 충분)
- **조기 실패 통보**: 분리 직후 베이스 스템 RMS가 너무 낮거나, 비트가 안 잡히면
  채보(9~14분)를 시작하기 전에 알린다. 라이브 직캠 실측에서 베이스 RMS가
  0.026(정상 곡의 1/6)이었던 사례가 있다
- 캐시 히트는 이미 동작한다(`find_cached`) — 재방문 경로를 UI에서 안내

### 3. 조성 확신도 임계 — **끝났다** (2026-08-07)

`KeyEstimate.trusted` + `reason`을 추가했고, `jobs._load_key_signature()`가
`trusted=False`면 조표를 넘기지 않는다. 조성 이름은 manifest에 남으므로 UI에서
참고로 보여줄 수는 있다.

**임계를 추측으로 정하지 않았다 — 상한이 정해져 있었다.**

실곡에서 우리가 낸 E major의 다이어토닉 비율이 **0.707**, 확신도 **1.065**였다.
그 답은 Songsterr 사람 채보로 **검증됐다**(원곡 A major, 커버는 완전4도 아래).
즉 **0.707과 1.065는 맞는 답의 값**이고, 임계를 그보다 높게 두면 정답을 버린다.

- `MIN_DIATONIC_RATIO = 0.60` — 검증된 0.707 아래
- `MIN_KEY_CONFIDENCE = 1.02` — 나란한조·딸림조는 구성음이 6개 겹쳐 정답이라도
  이 비율이 1에 가깝다. 동점(1.0)만 배제한다

더 조이려면 조표 정답이 있는 곡이 필요하다. Songsterr 채보 중 `keySignature`를
담은 것이 있다(Come Together = 2샵 major) — 골든셋이 늘면 다시 잰다.

### 4. 워크다운(하행 라인) 보존 — 초급판이 음악적으로 죽는 지점

**왜** — 마디에 순차 하행(C→B→A→G)이 있으면 `bar_root`가 지배 피치클래스 하나를
골라 템플릿 전체에 깔아 **평평해진다.** 경과음은 도수 2·5라 초급의
`keep_intervals`(0·7)에 없어 근음으로 대체된다. 워크다운은 베이스의 핵심
장치인데 통째로 사라진다. 앵커와 5도·옥타브 규칙으로도 안 잡힌다.

- 마디 안에서 **순차 진행(인접 반음·온음 이동이 이어지는 구간)**을 감지
- 감지되면 그 구간은 템플릿으로 덮지 않고 원곡 음정 윤곽을 유지
- 다음 마디 근음으로 향하는 하행이면 특히 살린다(마디 연결이 매끄러워진다)

### 5. 고스트 노트 측정 — 위험 크기를 아직 모른다

**왜** — CREPE는 퍼커시브한 데드 노트에도 억지로 피치를 붙인다. 그 음이 하향에서
**근음으로 대체되어 실제로 울려야 할 음으로 둔갑한다** — 삭제보다 나쁠 수 있다.
그리고 오늘 넣은 음량 게이트가 뮤트음을 버릴 위험을 키웠다.

- IDMT 어노테이션에 `excitationStyle`·`expressionStyle`이 있다. 데드 노트가
  어떤 피치로 잡히는지 **잴 수 있다**
- 재기 전에 대응을 설계하지 말 것. 크기를 모르면 과잉 대응이 된다
- 대응 후보: 게이트가 버린 음을 X표 후보로 기록 / 슬랩·펑크 의심 곡에서 게이트 완화
- alphaTex의 고스트 노트(X표) 문법은 **아직 실측하지 않았다**

### 6. 자동 넘김 뷰 + PDF (VIEW-02·03, EXP-01)

**왜** — 재생 중에는 커서를 따라 자동 스크롤된다. 문제는 **재생 없이 혼자 느리게
연습할 때**다. 두 손이 묶인 상태에서 마디마다 마우스를 잡아야 한다. 그리고
초급자는 태블릿에 띄우거나 프린트해서 쓴다.

- 4마디만 크게 띄우고 커서 진행에 따라 창 이동 (요구사항 3번의 나머지 절반)
- PDF는 alphaTab 인쇄 경로로 비교적 싸게 붙는다

### 7. 곡 구조 표시 (Verse/Chorus) — 못 보던 사각지대

**왜** — 초급 출력은 균일 8분이라 **모든 마디가 똑같이 생겼다.** 마디 번호는
있지만 단락 표시가 없어서, 스크롤을 놓치면 지금 1절인지 2절인지 알 수 없다.

- 보컬 멜로디 단(SCR-14)이 상당 부분 해결한다 — 그쪽을 먼저 해도 된다
- 구조 검출은 별개 난제다. 반복 구간 검출부터 재야 한다

### 7.5 구조 분할(sections.py)을 켤지 판정 — 정답 한 곡이 더 필요하다

**왜** — `pipeline/sections.py`를 만들어 관성 창을 곡 구조 경계로 자를 수 있게
했다(리듬 특징 SSM + 노벨티 + 4배수 스냅 + 8마디 하한). 경계를 **잘 찾는다** —
실곡에서 1·17·25·41·49·57·65·73·89·97마디를 냈고 49·57은 속주 구간(49~55)의
정확한 경계다. 그런데 켜면 결과가 나빠진다:

| 창 | 25~40 타현 | 41~99 타현 |
|---|---|---|
| 고정 12마디 (기본) | **16/16 100%** | **27/43 63%** |
| 구조 분할 | 14/16 88% | 23/43 53% |

**진단(교차 검증에서 합의)**: 경계 정확도와 투표 안정성이 상충한다. 섹션이
8~16마디로 짧아지면 최빈 패턴 투표의 표본이 줄어 흔들리는 검출에서 불안정해진다.
고정 창은 경계가 틀린 대신 표본이 항상 충분하다.

**해결 후보 — 섹션 클러스터 투표**: 1차로 경계를 찾고, 섹션끼리 리듬 특징
유사도로 묶어(1절 벌스와 2절 벌스를 같은 묶음으로) **묶음 전체에서 투표**한다.
경계는 정확하게 유지하면서 표본을 늘린다.

이 곡은 100마디 넘게 같은 4마디 그루브를 반복해서 나눌 구조가 애초에 없다.
**벌스와 코러스의 밀도가 실제로 다른 곡의 정답이 없으면 판정할 수 없다.**
그래서 8번(골든셋 확대)에 막혀 있다. `inertia.USE_SECTIONS = False`.

### 7.7 악보 편집 — 시장 조사에서 나온 두 번째 갭

**왜** — Klangio는 브라우저에서 음을 옮기고 박자표·템포·조성을 고칠 수 있다.
우리는 못 한다. 타현 정확도 73%에서 편집 수단이 없으면 사용자가 틀린 곳을 봐도
손댈 방법이 없다. 내보내기가 붙었으니 **MuseScore로 나가서 고치는 경로는
열렸지만**, 앱 안에서 한 음 고치는 것과 다른 프로그램을 켜는 것은 비용이 다르다.

싼 것부터: 마디 하나를 지우기 / 한 음의 프렛을 ±1 / 타현 하나 추가·삭제.
alphaTab은 렌더러라 편집을 대신 해주지 않으므로, 우리 `notes.json`을 고치고
다시 조립하는 쪽이 맞다(`compose.build`가 이미 그 구조다).

### 7.8 코드 진행 표시 — 이미 있는 데이터를 안 쓰고 있다

**왜** — `chords.py`가 마디별 코드를 검출해 놓고 악보 심볼로만 붙인다. 베이스
연주자에게 코드 진행은 핵심 정보이고 Chordify는 이것만으로 서비스를 한다.
새로 계산할 것이 없다 — `chords.json`을 UI에 띄우면 된다.

### 8. 골든셋 확대 — 위 전부의 회귀 안전망

**왜** — 지금 어떤 변경이 실사용 경로 품질을 떨어뜨려도, 합성 픽스처 3종과
베이스 단독 IDMT로만 잡힌다. **분리 단계를 포함한 경로는 무방비다.** 그리고
7도 코드 복구·레벨별 품질 점수·LLM 비교가 전부 이것에 막혀 있다.

- 평가는 근음 일치율(코드 진행만 있으면 되므로 곡 붙이는 비용이 낮다)
- **Songsterr의 사람 채보를 쓸 수 있다.** `tools/fetch_songsterr.py`로 받는다.
  튜닝이 우리 standard와 같고 리듬·타이·섹션 마커까지 있다. 곡 하나 붙이는
  비용이 판독보다 훨씬 낮다 — 페이지를 열어 CDN 토큰만 찾으면 된다
  (`eval/golden/songsterr_champagne_bass.json`이 첫 사례)
- 코드 진행 출처: Hooktheory TheoryTab, 국내곡은 2곳 이상 대조
- 참조 악보·영상 판독은 Gemini vision이 최적. **MCP `gemini_analyze_image`는
  어떤 이미지에도 400을 낸다** — `tools/gemini_vision.py`(REST 직통)를 쓴다.
  모델 이름은 `gemini-3.1-pro-preview`다(`gemini-3.1-pro`는 없는 이름, 404)

---

## 인사이트 — 판단할 때 참고할 것

### 게이트 판정 지표를 8분에서 16분으로 바꿨다 (오발동 35% → 12%)

옛 게이트는 "온셋의 8분 자리 정렬률 < 0.85"로 두 연주 겹침을 판정했다. IDMT
정답 17곡(한 사람 연주)에 걸어보니 **6곡(35%)에 발동**했다. 그중 대부분은 16분
정렬률이 0.98~1.00인 **정상 16비트 라인**이었다 — 8분 자리에 안 걸리는 게
당연한 연주를 겹침으로 오판하고 실음을 지우고 있었다.

지금은 **16분·8분셋잇단·16분셋잇단 중 가장 잘 맞는 격자**로 판정한다(임계 0.95).
격자마다 같은 여유(±0.0625박)를 쓴다 — 여유를 격자 간격에 비례시키면 거친
격자가 박 전체를 덮어 무엇이든 통과한다. 오발동 2/17(12%)로 내려갔다.

**판정과 감량은 다른 지표를 쓴다.** 발동 여부는 16분 격자로 보고, 얼마나 버릴지는
8분 격자로 수렴시킨다. 겹친 두 연주는 인접 16분 자리에 짝으로 앉아 16분 격자에
얹히므로, 16분을 목표로 삼으면 깎을 이유를 못 찾는다(실측: 344음 남음 대 300음).

### 정확도에 대한 정직한 프레이밍

피치 정확 85.3%는 **10개 중 1.5개가 틀린다**는 뜻이다. 베이스는 화음의 밑동이라
피치가 틀리면 밴드 전체 코드가 틀려 들린다. 초급자일수록 악보를 맹신한다.

**거짓음(9.1%)이 누락(13.7%)보다 치명적이다.** 없는 음을 치게 만드는 것이 빠진
음보다 나쁘다. 우리 평가 도구(`eval_practice.py`)는 이미 그 관점으로 재고 있는데
**제품에는 노출되지 않는다** — 마디별 신뢰도를 알면서 사용자에게 안 알려준다.

### 사용자가 실제로 넣을 입력이 우리가 가장 못 하는 것

연습 영상(원곡 반주 위에 커버 연주)은 유튜브에서 가장 많이 나오는 형태인데,
음량 게이트를 걸어도 8분 수렴이 75.7%로 목표(85%)에 못 닿는다. 감지·경고는
붙였지만 **품질 자체는 여전히 낮다** — 정답 59마디에서 타현 63%가 그 결과다.

### 슬랩 누락은 하향으로도 안 가려진다

슬랩 누락 51.7%. 하향 템플릿은 **검출된 음 수로 밀도를 정하므로**, 검출이 비면
초급판도 빈다. 귀에는 강하게 때리는데 악보는 쉼표 → 신뢰가 즉시 0이 된다.
"하향하면 오차가 지워진다"는 성질이 **누락에는 적용되지 않는다.**

### 변칙 튜닝 감지 부재

원곡이 반음 내림 튜닝으로 녹음된 것을 감지하지 못한다. 그러면 닫힌 포지션
운지가 나오고 "튜닝을 내리세요"라는 안내도 없다. 초급자에게는 쥐약이다.
(PRD 오픈이슈의 "튜닝 자동 감지"가 이것이고 P2로 미뤄져 있다)

### 교차 검증에서 **틀렸던** 지적 — 다시 검토하지 말 것

- **"리듬 기보를 alphaTab 자동에 맡긴다"** — 아니다. 정렬 규칙 표를 직접 갖고
  있고 박 경계를 넘으면 잘라 이음줄로 잇는다. `review_score.py`가 위반 0을 확인한다
- **"이조 시 운지 재계산이 안 될 것"** — 재계산한다. 프렛 상한 유지도 확인됐다
- **"초급이 코드 루트를 강제해 분수 코드를 파괴할 것"** — 하향은 코드를 모른다.
  `bar_root`가 고르는 것은 베이스가 실제로 친 최저음이다
- **"자동 스크롤이 없다"** — 재생 중에는 커서를 따라간다

---

## 지금 상태 (정합성 확인용)

구현·검증된 것은 `PLAN.md`에 근거와 수치가 있다. 요약만.

| 영역 | 상태 |
|---|---|
| 산출물 구조 | `notes.json` 원본 저장, 레벨·이조·튜닝 변형이 0ms |
| 리듬 격자 | 곡마다 8분/16분/셋잇단 적응 선택(임계 5%, IDMT 17곡 근거) |
| 난이도 | 3단계(초급·중급·원본), 하향 단방향, 불변식 4개 테스트 고정 |
| 매력 규칙 | 앵커(시그니처 당김음) + 근음·5도·옥타브 |
| 쉬운 곡 판정 | 원곡이 이미 초급 수준이면 단계를 안 만든다 |
| 키 | 재생 피치와 악보가 함께 이동, ±6반음, 튜닝 내림 대안 |
| 코드 | 분수 코드 지원, 7도는 끔(거짓양성 측정됨) |
| 조성 | 코드 진행으로 24조 채점, 조표 표기·이조 반영 |
| 두 베이스 입력 | 음량 게이트 + 감지·경고 + 신뢰 단계 안내 |
| 렌더 | 오선+TAB, 마디 폭 균등, 행당 4마디, 커서 동기화 |
| 패턴 관성 | 고정 12마디 창, 슬롯별 과반 투표, 필인·쉼표 보호. `compose.build()`에 배선됨 |
| 구조 분할 | `sections.py` 있음. **끔** — 경계는 맞게 찾는데 결과가 나빠진다(7.5 참조) |
| 회귀 | 코드 11건 / 하향 불변식 / 표기 / 조합 45개 / 정답 59마디 — 실패 0, 빌드 통과 |

## 백로그 (2026-08-09 사용자 지시)

### B-1. 유튜브 서비스 분석 → 클론 (베이스 초보 기능) [분석 완료 2026-08-09]
- 대상: https://youtu.be/S9wYFUunm_E — 영상 상세 분석 → 영상에 나온 서비스
  상세 분석 → 베이스 초보를 위한 기능 클론.
- 선행: B-2 스킬로 영상을 적재한 뒤 분석.
- **완료**: eval/bench/yt-S9wYFUunm_E-analysis.md — 무료 체인(Audacity→
  Neural Note→MuseScore) 영상. 클론 후보: 밴드 악보 뷰(하단 B-3),
  MuseScore 내보내기 노출 강화(소품), 파트별 신스 음색(후속).

### B-2. "유튜브 보고 적재" 스킬화 [완료 2026-08-09 — ~/.claude/skills/youtube-ingest]
- 방법론(사용자 제공 레퍼런스, moongi diary EP.26): 전부 로컬·무료 —
  ① yt-dlp: 영상+자막 다운로드
  ② ffmpeg: 프레임 잘라내기
  ③ Whisper: 자막 없으면 로컬 받아쓰기 (API 비용 0)
  ④ Pillow: 프레임을 보드로 타일링 → LLM 분석용 이미지 보드
- 산출: ~/.claude/skills/youtube-ingest (가칭) — 영상 URL → 프레임 보드 +
  대본 텍스트 → 서비스/UX 분석에 투입.
- 참고: 우리 파이프라인에 yt-dlp·ffmpeg·faster-whisper 이미 있음 — 조립만.

### B-3. 밴드 악보(멀티 트랙) 뷰
- 근거: B-1 분석 — 시청자 니즈의 본체가 "밴드 전체 악보". 현재 보컬+베이스
  2트랙에 드럼 리듬 슬래시 트랙, other(기타·건반) 코드 트랙 추가.
- 드럼 채보 파이프라인(온셋→킥/스네어 분류)이 선행 과제.
