# HANDOFF — 다음 세션 재개용 (2026-08-07)

유튜브 링크 → 베이스 자동 채보 + 연습 플레이어 (Lowend).
**재개 시 이 파일부터 읽고, 그다음 `NEXT.md`로 간다.**

---

## 한 문장 요약

파이프라인은 전 구간 관통하고 웹에서 동작하지만, **골든셋을 4곡으로 늘리자
한 곡에서만 쓸 만하고 나머지 세 곡에서 무너진다는 것이 드러났다.**

---

## 바로 다음에 할 일 (순서대로)

### 1. 연습영상 오판 고치기 — 지금 사용자에게 틀린 정보를 보여주는 중

**증상**: Queen·Beatles·Jamiroquai **공식 스튜디오 음원 3곡을 전부
`practiceVideo=True`로 판정**한다. 그러면 (a) 하향 단계를 안 만들고
(b) "베이스가 둘 섞여 있습니다"라는 틀린 경고를 띄운다.

**원인**: `pipeline/diagnose.py`가 게이트 후 격자 정렬률만 본다. 정렬이 나쁜
이유가 "베이스가 둘"이 아니라 **"우리 검출이 나빠서"**일 수 있는데 구분하지
못한다. 보조 신호인 어택 편차도 세 곡에서 1.39~1.50으로 기존 실곡보다 높아
**못 가른다.**

**방향**: 판정을 보수적으로 바꾼다 — 확신 없으면 원곡으로 본다. 틀린 경고는
없는 경고보다 나쁘다. 검증은 골든셋 4곡에서 `practiceVideo`가 어떻게 나오는지
보면 된다(정답: 커버 영상 1곡만 True).

**주의**: 이 판정을 느슨하게 하면 실제 연습 영상에서 하향 단계가 다시 열린다.
그 경우 하향 품질이 낮다는 근거는 `diagnose.py` 머리말에 있다.

### 2. 배음을 기음으로 잡는 것 (Come Together)

박자 4/4 ✓, 마디 89/89 ✓, BPM 83.3 ✓ — **시간축은 정확한데 음이 틀린다.**
최적 이조가 +7반음(완전5도), 상관 0.727 / 0반음 0.642로 애매하다. 일정한
이조가 아니라 **일부 음에서 3배음을 f0로 잡는** 모습이다.

리켄배커 + 피크 연주는 기음보다 배음이 강하게 녹음되는 전형적 경우다. CREPE는
단선율이라 배음을 별도 음으로 만들지 않지만 **기음보다 배음이 강하면 그쪽을
고른다.**

**방향**: `transcribe_crepe`에서 f0 후보의 1/2·1/3 지점 에너지를 확인해 기음을
되찾는다. `fmax=500`을 낮추는 것도 후보지만 슬랩 고음을 잃는다 — 재고 정한다.

### 3. 16비트 곡에서 검출이 붕괴하는 것 (Virtual Insanity) — 가장 크고 어렵다

- 234초 곡에서 **88음.** 마디당 8~12타여야 하니 **10배 누락**
- **박자를 3/4로 오검출**(정답 4/4). 이것 하나로 모든 마디 귀속이 깨진다
- 피치 상관은 0.692 — **피치는 보는데 음을 못 찾는다**

박자부터 봐야 한다. `beats.py`가 4/4를 3/4로 잡는 원인을 먼저 규명한다.

---

## 지금 정확도 (골든셋 4곡)

| 곡 | 음(피치클래스) | 타현 | 평균오차 | 음원-악보 이조 |
|---|---|---|---|---|
| Champagne Supernova (커버 영상) | **93%** | **63%** | 0.51 | 0 |
| Queen — Another One Bites the Dust | 62% | 4% | 2.50 | **+1반음** |
| The Beatles — Come Together | 28% | 3% | 3.50 | +7(불확실) |
| Jamiroquai — Virtual Insanity | 16% | 0% | 6.62 | 0 |

재현:

```bash
# 커버 영상 (마디별 정답 59마디)
.venv/Scripts/python.exe eval/eval_video_bars.py data/975e4e588d282666 \
    eval/golden/champagne_video_bars41_99.json

# 골든셋 3곡 (Songsterr 사람 채보)
.venv/Scripts/python.exe eval/eval_songsterr.py data/528aa2e6986aa42a \
    eval/golden/songsterr_queen_aobtd.json
.venv/Scripts/python.exe eval/eval_songsterr.py data/78d6e3fc12388629 \
    eval/golden/songsterr_beatles_come_together.json
.venv/Scripts/python.exe eval/eval_songsterr.py data/c54d965e0a8fda45 \
    eval/golden/songsterr_jamiroquai_virtual_insanity.json
```

`data/`는 gitignore이므로 **클론 직후에는 이 해시들이 없다.**
`eval/golden/SET.md`에 유튜브 URL과 재생성 절차가 있다(곡당 약 8~11분).

---

## 이번 세션에서 한 것

| 영역 | 결과 |
|---|---|
| 게이트 판정 지표 교체 | 정상 곡 오발동 **35% → 12%** (8분 격자 → 16분·셋잇단 중 최선) |
| 패턴 관성 배선 | `compose.build()`에 들어감. 그전에는 측정 스크립트로만 검증돼 있었다 |
| 정답 확장 | 16마디 → **59마디** (커버 영상) + Songsterr 사람 채보 4곡 |
| 내보내기 | **MusicXML(현·프렛 포함) · MIDI.** 시장 조사 최대 갭이었다 |
| 조성 확신도 임계 | `trusted` 플래그. 임계는 검증된 정답값(0.707) **아래로** 잡았다 |
| 킥 동기화 | 만들었고 첫 곡에서 발동 안 함(락 6.1%). 판정은 골든셋으로 미룸 |
| 규칙값 적재 | `POLICY.md` + `tools/audit_constants.py`. 근거 없는 상수 **30 → 3개** |
| 경쟁 조사 | `MARKET.md`. 유튜브→베이스 TAB은 이미 상용 3사가 한다 |

## 측정에서 진 시도 — 다시 하지 마라

`NEXT.md`에 표로 있다. 요약:

- 관성 창을 구조 분할 경계로 (63% → 53%)
- 게이트 마디 보호에 16분 조건 추가 (73% → 53%)
- 겹침을 "일정 시간차 짝 온셋"으로 판정 (신호가 전체의 8%뿐)
- 커버/반주를 음량으로, 어택 선명도로 가르기 (둘 다 실패)
- 음 쪼개짐 병합 (26% → 30%, 쪼개짐은 원인이 아니다)
- **재타현 분리(스펙트럼 플럭스)** — 쓸 수 있는 중간값이 없다. 40/47 유지 아니면 0/47 붕괴

## 내가 두 번 저지른 실수 — 측정 도구를 먼저 의심하라

1. **`bassclean._bar_of`로 피치 귀속을 분석**해 "다른 피치 끼어듦 21%"라는
   없는 문제를 만들었다. 그 함수 독스트링에 "위상은 정확하지 않아도 된다"고
   적혀 있었다. `quantize` 기준으로 다시 재니 **0%**였다.
2. **대조 도구가 이조를 못 찾아** Queen이 5%로 나왔고 파이프라인 붕괴로 봤다.
   실제로는 **유튜브 음원이 악보보다 반음 높았다.** 보정하니 62%.

숫자가 예상 밖으로 나쁘면 **파이프라인보다 측정 도구를 먼저 본다.**

---

## 문서 지도

| 파일 | 무엇이 있나 |
|---|---|
| `NEXT.md` | **우선순위와 진 시도.** 이 파일 다음에 읽는다 |
| `POLICY.md` | 규칙·정책 상수 111개의 근거 등급. 값을 바꾸기 전에 본다 |
| `MARKET.md` | 경쟁 조사·벤치마크. 무엇이 차별점이고 무엇이 아닌지 |
| `eval/golden/SET.md` | 골든셋 곡 목록·URL·추가 절차 |
| `PLAN.md` | 세션별 실행 기록과 측정치 |
| `PRD.md` | 서비스 정의·요구사항·13장 과제 나래비 |
| `CLAUDE.md` | 재발견하지 말아야 할 확정 사실과 **함정 모음** |

## 아직 없는 기능 (사용자 최초 요구 기준)

- **3단 악보(보컬 멜로디 + 가사)** — 보컬 496음은 뽑아뒀고 렌더·가사가 없다
- **4마디 자동 넘김 뷰**
- **PDF** — MusicXML·MIDI는 이번에 됐다. PDF는 alphaTab 인쇄 경로가 싸다
- **A-B 루프 · 메트로놈** — 없으면 뷰어지 연습 도구가 아니다
- **처리 15분 대기 UX** — 브라우저 닫으면 끝난다
- **악보 편집** — 정확도가 낮은데 고칠 수단이 없다
