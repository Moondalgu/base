# HANDOFF — 다음 세션 재개용 (2026-08-04)

작업: 유튜브 링크 → 베이스 자동 채보 + 연습 플레이어 (Lowend).
이 문서는 진행 상태·점검 결과·다음 확인 목록이다. 재개 시 이 파일부터 읽는다.

## 현재 상태 요약

- **전 변경 미커밋** (encode.py 신규 + bassclean.py/quantize.py 포함 10개 수정)
- 실곡 1건: `data/975e4e588d282666` — 백예린 Champagne Supernova 베이스 커버 (5:36, 75BPM). **수정 반영해 제자리 재생성 완료** (quality 43→47/reference, subdivision 3→4, 스템 캐시 유지)
- 사용자 피드백 ①악보 표기 오류 ②통상적이지 못한 형태 ③커서 → **①②는 아래 "악보 품질" 절대로 해결**, ③은 코드 반영했으나 여전히 미검증

## 악보 품질 (2026-08-04 규명·수정 완료)

### 측정 기준선 — 픽스처 3종, 회귀 감시용
| 픽스처 | 수정 전 | 수정 후 |
|---|---|---|
| bass_only | F1 0.931 | **0.947** (오검출 1건 제거로 P=1.000) |
| mix | F1 0.815 | 0.815 (불변) |
| mix_swing | F1 0.951 | 0.951 (불변, 스윙 판정 유지) |

> `bass_only` F1이 0.036으로 나오면 회귀가 아니라 **오래된 산출물에 `phase`가 없는 것**이다(PRD A.9). 재생성하면 복구된다.

### 해결 1 — 전곡 셋잇단화 (이벤트 83.9% → 0%)
`_detect_swing`이 스트레이트 곡을 스윙으로 오판해 `subdivision=3`이 선택되고 있었다. **유령음 탓이 아니다**: 유령음 제외·고신뢰음만(amp≥0.6)·피치필터 어떤 부분집합에서도 오프비트 중앙값이 0.74 이상으로 나왔다. 온셋이 박 안에서 진짜로 늦게 몰린다는 뜻이고, 원인은 **비트 그리드 어긋남**(`bpm_variance` 0.0755, 비트간격 0.180~1.460s).

→ `quantize.SWING_MAX_BPM_VARIANCE = 0.05` 게이트 추가. 불안정한 그리드에서는 스윙을 추론하지 않는다. mix_swing(0.0374) 통과 / 실곡(0.0755) 차단. **마진이 넉넉하지 않으므로 골든셋 확보 후 재검토 대상.**

### 해결 2 — 고프렛 유령음 (1~31마디, 원본 악보상 전부 쉼표)
| | 수정 전 | 수정 후 |
|---|---|---|
| 유령 구간 음표 | 95개 | 23개 |
| 12프렛 이상 비율 | 90.5% | **0%** |
| 실연주 구간 음표 | 83개 | 80개 (최대프렛 12→4) |

기존 필터가 못 잡은 이유: 배음 제거는 기준이 될 낮은 실음이 없는 구간에서 무력하고, 음역 필터는 상한 초과를 **옥타브 접어 살리기** 때문에 `dropped_out_of_range`가 실측 798음 중 항상 0이었다.

→ `bassclean`에 누출 제거 단계(step 4) 추가. 절대 피치 상수를 쓰지 않고 **고신뢰음 중앙피치 + 14반음**을 곡마다 상한으로 추정, 그보다 위이면서 약한(amp<0.85) 음만 폐기. 고신뢰음이 없으면 단계를 건너뛴다.

### 부수 수정
- 병합 판정을 `detected_end`(basic-pitch 원래 검출 끝) 기준으로 변경 — step 4가 잘라낸 end를 쓰면 포함관계 음까지 붙는다. 실효는 작다(실곡 202→200음).
- `mono.pop()` 손실이 `CleanReport`에 집계되지 않던 것 → `dropped_truncated` 추가. `dropped_leakage`도 추가(관측용, `quality.py`엔 미반영).

### 틀렸던 가설 (같은 실수 반복 방지)
- **"병합이 실연주 84%를 삼킨다"는 오측정이었다.** 체인의 첫 음 기준으로 간격을 재서 "체인이 길다"를 "별개 음이 병합됐다"로 오독했다. 인접쌍 단위로는 466쌍 중 431쌍이 0~40ms로 진짜 맞닿아 있고 음수(포함관계)는 1건뿐. basic-pitch가 지속음을 0.17s 조각으로 쪼개고 병합이 되붙이는 정상 동작이다. 실곡은 "초보·쉬운버전" 75BPM 발라드라 마디당 1.3~1.4음이 맞을 수 있고, **정답이 없어 recall을 판정할 수 없다.**
- **"운지 고프렛 편향"도 실재하지 않았다.** 실연주 구간 프렛 중앙값 0. 고프렛은 전부 유령음이었다. tuttut 가중치 튜닝 불필요.
- PRD 4.11의 "병목은 스템 분리다"는 합성 픽스처 기준이다. 실곡의 병목은 **비트 추적**이다.

### 해결 3 — 루트 페달 병합 (온셋 간격 조건)
같은 피치 병합이 `note.start - prev.detected_end <= 40ms`만 봐서, 75BPM 8분음표 어택(0.4초 간격)까지 하나로 뭉갰다. basic-pitch가 검출한 앞 음의 끝이 다음 어택 온셋까지 이어지므로 **간격이 0/음수로 나와 "조각"과 "재타격"이 구분되지 않는다.**

→ `bassclean`에 온셋 간격 조건 추가: `note.start - prev.start < min_attack_interval`. 호출부에서 `(60/BPM)/4`(16분음표)를 넘긴다. 실측 근거: 같은 피치 연속 온셋 간격이 조각 0.070~0.139초 vs 재타격 0.592초 이상으로 깨끗하게 갈렸다.

| 픽스처 | 이전 | 이후 |
|---|---|---|
| bass_only | 0.947 | **0.983** |
| mix | 0.815 | **0.885** (recall 0.733→0.900) |
| mix_swing | 0.951 | 0.935 (**소폭 회귀**) |

mix_swing 회귀 원인: 긴 지속음이 **늦게** 쪼개진 조각(온셋 간격 0.325초, 길이 0.069초)이 그리드 폭(0.125초)보다 넓게 벌어져 재타격으로 오인된다. "두 번째 조각이 아주 짧고 약하면 조각으로 본다"는 조건을 더 붙이면 잡힐 것으로 보인다. **미해결.**

실곡: 음표 103→359, 품질 47→56, `densityConsistency` 0.039→0.429.

## ★ 처음으로 신뢰할 수 있는 정확도 측정 (2026-08-04)

**자산**: `IDMT-SMT-BASS-SINGLE-TRACKS` — `data/_datasets/idmt_single/` (Zenodo 7544099, 20.5MB, CC BY-NC-ND 4.0, 신청 절차 없음)
- **실제 4현 일렉베이스 17곡, 5.9분, 948음**. 튜닝 `28,33,38,43` = 우리 standard와 동일
- 어노테이션 XML에 **onsetSec / offsetSec / pitch / stringNumber / fretNumber / excitationStyle / expressionStyle**. 운지 평가까지 가능
- IDMT 현번호는 **1=E(28), 2=A(33), 3=D(38), 4=G(43)** — alphaTex와 **반대** 방향이므로 운지 대조 시 변환 필요
- 베이스 단독(믹스 없음) → 분리 단계는 못 재지만 **채보 자체의 상한**을 잰다
- 측정 스크립트: 세션 스크래치패드 `eval_idmt.py`, `eval_idmt_steps.py`

### 결과 — 온셋+음높이, 허용 50ms (mir_eval)

| 단계 | P | R | F | ΔF | 버린 음 |
|---|---|---|---|---|---|
| basic-pitch 원본 | 0.289 | 0.865 | 0.432 | — | 0 |
| 1) 배음 제거 | 0.566 | 0.862 | **0.681** | **+0.249** | 1396 |
| 2) 음역 필터 | 0.565 | 0.859 | 0.679 | −0.002 | **0** |
| 3) 짧음·약함 | 0.568 | 0.859 | 0.682 | +0.002 | 8 |
| 4) 누출 제거 | 0.596 | 0.857 | 0.698 | +0.017 | 68 |
| 5) 단선율 강제 | 0.688 | 0.815 | **0.743** | +0.044 | 254 |
| 6) 병합 | 0.696 | 0.716 | 0.705 | **−0.038** | 148 |

**외부 기준**(FiloBass 논문, 콘트라베이스, 동일 허용오차): basic-pitch 0.627 / CREPE Notes 0.729 / Melodyne 0.790. 데이터셋·악기가 달라 직접 비교는 아니지만 우리 0.743이 그 구간 안에 있다.

### 확정된 사실
- **배음 제거가 압도적 최대 이득**(+0.249). recall을 거의 안 잃고(0.865→0.862) precision을 두 배로 올린다. basic-pitch는 948음에 2846개를 뱉는(3배 과검출) 모델이고, 이 단계가 그걸 잡는다.
- **음역 필터는 아무것도 안 한다**(버린 음 0). 옥타브 접기 때문에 상한이 무력하다는 진단이 수치로 확인됐다.
- **누출 제거는 솔로 베이스에서도 68음을 버리는데 F가 오른다**(+0.017). 과잉이 아니다 — 우려 해소.
- **병합만 유일하게 F를 떨어뜨린다**(−0.038). recall 10pp를 내주고 precision은 1pp만 얻는다.

### 주법별 (최종 F)
| 주법 | 곡수 | F |
|---|---|---|
| PK (picked) | 3 | **0.836** |
| FS (finger style) | 7 | 0.736 |
| MU (muted) | 4 | 0.716 |
| **SP/ST (slap)** | 3 | **0.484** |

슬랩이 명확한 약점이다. PRD 4.8은 슬랩을 "표기하지 않는다"로 정했지만, **검출 자체가 안 되는 문제**이기도 하다.

### 미해결 — 병합 임계값은 진짜 트레이드오프다
두 데이터셋이 반대 방향을 가리킨다.

| min_attack_interval | 병합없음 | 0.05 | 0.08 | 0.125 | 0.15(현재) | 0.25 |
|---|---|---|---|---|---|---|
| IDMT 17곡 F | **0.743** | 0.743 | 0.728 | — | 0.705 | 0.675 |
| bass_only F | 0.951 | 0.951 | 0.951 | 0.983 | **0.983** | — |
| mix F | 0.829 | 0.829 | 0.841 | 0.885 | **0.900** | — |
| mix_swing F | 0.848 | 0.848 | 0.848 | 0.855 | **0.855** | — |

합성 픽스처는 지속음을 basic-pitch가 쪼개므로 병합이 이득이고, 실연주는 같은 음 반복이 많아 병합이 손해다. **임계값으로는 못 푼다.** 필요한 것은 더 나은 판별자다.

후보(미검증): **재타격은 진폭이 다시 오르고, 조각은 앞 조각보다 진폭이 낮다.** basic-pitch가 노트별 amplitude를 주므로 "앞 음 대비 진폭 비율"로 가를 수 있는지 이 데이터셋으로 검증 가능하다(정답이 있으니 판별자 자체를 채점할 수 있다).

## 쓸 수 있는 측정 지표 — 근음 일치율 (2026-08-04)

정답 악보가 **편곡**이라 리듬·옥타브는 못 믿지만 **코드 근음은 안 바뀐다.** 그래서 "마디별 지배 피치클래스가 코드 근음과 맞는가"를 재면, 편곡 차이·리듬 오차·옥타브 선택에 흔들리지 않는 정직한 지표가 된다. 무작위 기대치가 12분의 1(8.3%)이라 우연과 구분도 쉽다.

자료: `akbobada.com` PDF(`C:\Users\admin\Desktop\악보보기 _ 악보바다.pdf`) — **4현 베이스 TAB**(위쪽 오선은 가사 붙은 보컬 멜로디 참고용), E 메이저, 4/4, ♩=78, TAB 숫자는 전부 A현 7·5·4·2 = 근음 E·D·C#·B, 마디당 8개(8분음표 페달), TAB에 기둥·꼬리 직접 표기. "동영상 F7Z6SFr-abQ 버전으로 제작 / 4현 베이스로 연주 가능하도록 편곡".
근음 주기: 4마디부터 **D → C# → B → E** 4마디 반복.

| 소스 | 근음 일치율 | 무작위 대비 |
|---|---|---|
| 커버 영상 `975e...`(베이스 2개 섞임) | **78.0%** (64/82) | 9.4배 |
| 라이브 직캠 `c7b9...`(베이스 1개) | **56.4%** (53/94) | 6.8배 |

오프셋 스캔에서 최적점이 뾰족하게 나온다(78% vs 나머지 14.6/2.4/0%) → **마디 정렬도 함께 검증된다.** 이 지표는 코드 진행만 있으면 어느 곡에나 쓸 수 있으므로 골든셋 확보 부담이 크게 줄어든다.

**주의**: 직접 만든 진단 스크립트에서 alphaTex 현 번호를 뒤집어 매핑해 처음에 12~18%가 나왔다. 올바른 매핑은 `TUNING_PRESETS['standard'] = [43(G), 38(D), 33(A), 28(E)]`에 대해 **`tuning[string - 1]`** (현 1=G, 4=E). `run_eval.py`는 올바르게 하고 있다.

## 라이브 직캠은 더 나쁜 소스였다 (2026-08-04)

"베이스가 하나인 소스가 필요하다"며 PDF 안의 링크(`F7Z6SFr-abQ` = 백예린 렛츠락 2017 4K 직캠, 320초)를 돌렸는데 **오히려 나빠졌다.**

| | 커버 영상 | 라이브 직캠 |
|---|---|---|
| 품질 점수 | 55 (reference) | **41** (reference) |
| 베이스 스템 RMS | 0.149 | **0.026** (6배 작음) |
| 베이스가 들리는 시간 | — | **24.7%** |
| `bpm_variance` | 0.076 | **0.344** |
| 강한 온셋 수 | 287 | 138 |

라이브는 관객·홀 울림이 `other` 스템을 지배하고(RMS 0.087, 95.7% 시간) 베이스가 묻힌다. 템포도 진짜로 흔들린다(라이브 밴드, 클릭 없음). **즉 "베이스 2개 섞임"보다 "베이스 1개가 묻힘"이 더 치명적이다.**

**균일 격자는 라이브에서 스스로 채택을 거부했다**(정렬오차 5.035 vs 5.282초) — 안전장치가 작동한 것은 맞다. 다만 두 값 모두 5초대인 것은 **온셋이 마디 수에 비해 너무 희박해 지표 자체가 무의미**했다는 뜻이다(138개 온셋 / 104마디). 이 축퇴 상황을 감지해 "판정 불가"로 명시하는 처리가 없다 — 지금은 우연히 안전한 쪽으로 떨어졌다. **미해결 견고성 과제.**

## 실곡 커버 영상의 성격 — 오디오에 베이스가 둘이다 (2026-08-04, 사용자 확인)

`data/975e4e588d282666`(백예린 Champagne Supernova)은 **베이스 커버 녹음이 아니다.** 원곡 음원을 반주로 틀고 그 위에 커버 베이시스트가 연주한 **연습 영상**이다. 화면 위쪽에 연주자, 아래쪽에 베이스 단독 악보, 악보 위에 파란 세로 커서가 재생 위치를 따라간다. 악보와 커서는 **커버 베이시스트의 파트**를 가리킨다.

**사용자 확인: 원곡 베이시스트와 커버 베이시스트의 주법과 타점이 서로 다르다.**

즉 **오디오에 베이스가 둘 섞여 있다.** Demucs는 둘을 구분하지 않고 하나의 `bass` 스템으로 합친다. 그래서 이 스템을 채보한 결과를 영상 악보와 비교하면 "커버 베이시스트만 적힌 정답" 대 "두 연주가 섞인 입력"을 대조하는 셈이라 **측정 자체가 성립하지 않는다.**

근거:
- **보컬 스템에서 뚜렷한 소리가 나는 시간 59.9%** → 반주가 보컬 제거된 MR이 아니라 원곡 음원이다. 따라서 원곡 베이스도 들어 있다.
- **마디 시작 12곳의 소리 상승 시간이 26~128ms로 5배 흔들리고, 짧은 것(26·35·38ms)과 긴 것(96·107·122·128ms)이 거의 번갈아 나온다.** 단일 베이스 핑거스타일 어택은 통상 20~60ms에 매끄럽게 오른다. 두 연주의 시간차가 마디마다 벌어지고 좁아지는 모습과 부합.
- 영상 구성은 Gemini 판독으로 확인(연주자 영상 + 베이스 단독 악보 + 파란 재생 커서).

### 이 발견이 뒤집는 것
1. **"영상 악보가 실제 연주보다 과하게 적혀 있다"는 이전 판단은 철회한다.** 뭉개진 엔벨로프는 악보 탓이 아니라 두 연주가 겹친 탓으로 설명된다. 악보는 커버 베이시스트 파트를 정확히 적은 것일 수 있다.
2. **이 곡으로 낸 수치는 파이프라인 성능이 아니다.** 어택 재현율 29.2%, 표기 손실 44/64 슬롯 등은 "두 연주가 섞인 입력"에 대한 값이다. "우리가 못하는 것"과 "소스가 이상한 것"이 구분되지 않는다.
3. **"음높이 100% 정확"도 과대평가였다.** 두 베이스가 같은 음을 치므로 음높이는 살아남는다. 게다가 대조한 4마디는 루트 페달(가장 쉬운 케이스)이었다.

### 그래서 필요한 것
- **베이스가 하나만 있는 소스로 다시 재야 한다.** 원곡 음원이거나 단독 베이스 녹음.
- 다만 이 곡은 버리지 않는다. **연습 영상은 실제 사용자가 넣을 가능성이 높은 입력**이고, 화면 커서에서 정답 타이밍을 프레임 단위로 읽어낼 수 있어(현재는 오디오만 받아둔 상태) 나중에 하드 케이스 사례로 가치가 있다.
- 제품 관점: 이런 입력을 감지해 경고하는 것이 조용히 엉망인 악보를 내는 것보다 낫다. 감지 신호는 위 어택 상승시간 편차가 후보다.

## 이전 기록 — 영상 TAB의 함정 (원인을 잘못 짚었던 단계)

`eval/golden/champagne_bars25_28.json`은 영상 자체 TAB을 Gemini로 판독한 것이다. **마디 번호·피치·조성은 신뢰할 수 있다**(우리 출력과 4마디 전부 피치 일치로 상호 검증). 그러나 **어택 개수·리듬 평가에는 쓸 수 없다.**

근거: 베이스 스템 RMS 엔벨로프 실측(25·26마디) 결과, 강한 어택 1~2회 뒤 슬롯 4→10에서 0.86→0.43으로 **매끄럽게 단조 감쇠**한다. 악보가 어택이라 표시한 슬롯 7·8·10에 재타격 흔적이 없다. 연주자는 음을 흘리고 악보는 8분음표 페달로 적어둔 것으로 보인다(Gemini도 이 악보를 "자동 생성처럼 보인다"고 판정 — 4분음표를 8분음표 2개+타이로 쓰는 Guitar Pro 습관).

→ `eval/compare_bars.py`의 어택 재현율(현재 20.8%)은 **이 악보 기준 수치이지 채보 실패율이 아니다.** 오디오 기준 정답이 따로 필요하다.

## 온셋 검출 추가는 실패 (측정 후 도입 안 함)

루트 페달 어택을 되살리려고 온셋 검출 3종을 실곡 25~28마디에서 비교했다(허용 70ms).

| 방식 | 온셋 수 | P | R | F |
|---|---|---|---|---|
| 현재 파이프라인 | 15 | 0.200 | 0.125 | 0.154 |
| librosa `onset_detect` | 53 | 0.264 | 0.583 | 0.364 |
| **superflux (최대필터)** | 75 | 0.267 | 0.833 | 0.404 |
| superflux + 16분 그리드 스냅 | 53 | 0.396 | 0.875 | 0.545 |
| **전 슬롯을 어택으로 찍기(상한)** | 64 | 0.375 | 1.000 | **0.545** |

**최선의 조합이 "모든 슬롯을 무조건 어택으로 찍기"와 F값이 같다(0.545).** precision이 우연 수준(0.396 vs 0.375)이라 오디오에서 어택 정보를 뽑지 못한다는 뜻이다. 파이프라인에 도입하지 않았다. 위 "골든 정답의 함정"과 합치면, 애초에 그 어택들이 오디오에 없을 가능성이 크다.

## 그리드 정렬 — 구현 완료 (2026-08-04)

`beats.py`에 균일 격자 피팅을 넣었다. **임계값으로 추측하지 않고 원본 격자와 정렬 오차를 직접 비교해 더 나을 때만 교체**한다(나빠질 수 없는 구조).

- `track_beats(..., phase_source=<베이스 스템>)`: 온셋 강도 엔벨로프에 대해 템포(median±7%, 0.05 간격)와 위상(0.01초 간격)을 훑어 마디 시작에 온셋 에너지가 가장 많이 걸리는 조합을 채택
- `BeatGrid`에 `uniform`/`fitted_bpm`/`fitted_phase`/`align_before`/`align_after` 추가(전부 기본값 있어 기존 `beats.json` 호환)
- **`bpm_variance`는 원본 검출 비트 기준을 유지한다.** 균일 격자로 0으로 재계산하면 ① `quality.beatStability`가 "비트 완벽"이라 거짓 보고 ② `quantize.SWING_MAX_BPM_VARIANCE` 게이트가 열려 전곡 셋잇단이 재발한다. 코드 주석에 이 이유가 있다.
- 활동 구간이 8마디보다 짧으면 피팅을 건너뛴다 → 16초 픽스처 3종은 자동으로 기존 동작 유지(비트 동일, F1 불변)

**실곡 결과**: 정렬 오차 **0.331초 → 0.064초**, 채택 템포 75.00 BPM(beat_this 값과 동일), 위상 +0.060초. 비트 간격이 전부 0.8초 하나로 균일해졌다(이전엔 마디 길이 1.86~4.02초). 셋잇단 0개·`subdiv=4`·`bpm_variance` 0.0755 유지.

**주의 — 품질 점수는 안 움직인다(56→55)**. `quantizationResidual`이 0.627→0.588로 오히려 조금 나빠지는데, 예전 울퉁불퉁한 격자가 연주 흔들림을 **따라다녀서** 잔차가 낮았던 것이다. 마디 길이가 제멋대로인 악보는 잔차가 낮아도 못 쓴다. **현재 품질 지표가 이 차이를 보상하지 못한다** — synctoolbox 되먹임 고리가 필요한 또 하나의 이유.

**마디 번호가 2 밀렸다**: 균일 격자를 t=0까지 확장해 앞에 빈 마디 2개가 생겼다. **악보 25마디 = 우리 27마디.** `eval/golden/champagne_bars25_28.json`에 `ourBarOffset: 2`로 기록하고 `compare_bars.py`가 적용한다(`--bar-offset`으로 덮어쓰기 가능). 오프셋 미적용 시 피치오류가 4건 나오는 것이 정렬 어긋남의 검증 신호다(우리 피치는 원래 오류 0).

**현재 대조 수치**(악보 25~28마디): 어택 재현율 **29.2%**(7/24), 피치오류 **0**, 마디 길이 검산 전 마디 정상, 표기 손실 44/64 슬롯. 격자 도입으로 20.8%→29.2%. 남은 격차는 격자가 아니라 **검출 단계에서 24타 중 17타가 아예 안 잡히는 것**이다.

### 원인 규명 기록 (프로토타입 단계)

**구조적 원인**: 우리는 격자를 먼저 만들고 오디오를 거기 욱여넣는다. DAW(Ableton Warp, Logic Flex Time, Melodyne)는 전부 **반대 순서** — 소리의 변화 지점(transient)을 먼저 확정하고 그것을 격자에 고정한 뒤 사이 구간만 보간한다.

**실측한 진짜 문제**: 템포는 맞았다. 최적 피팅 결과가 **75.00 BPM으로 beat_this median과 정확히 일치**. 문제는 `_build_grid`가 검출된 **개별 비트 사이를 각각 등분**하는 데 있다 — 비트 위치가 흔들려서 **마디 길이가 1.86~4.02초로 요동**친다(중앙값 3.20, 표준편차 0.177).

**프로토타입 결과** (베이스 스템 온셋 287개 대비 마디 시작 거리, 76~77마디):

| 방식 | 중앙값 | 평균 | 0.1초 내 |
|---|---|---|---|
| 현재(개별 비트 등분) | 0.368s | 0.331s | **22%** |
| 단일 템포+위상 균일 격자 | **0.025s** | 0.064s | **95%** |

위상 보정량은 +0.060초뿐이었다. 즉 위상보다 **격자 불균일성이 주범**이다. 스크립트: 세션 스크래치패드 `diag_gridfit.py` (탐색 범위 70~80BPM×0.05, 위상 0.01초 간격).

**주의**: 균일 격자는 템포가 일정한 곡에만 맞다. 루바토·템포 변화 곡은 구간별 재피팅(tatum tracking 계열의 로컬 그리드 재추정)이 정석.

### 리서치 결론 (4축 병렬, 2026-08-04)

- **우리 가설이 논문으로 검증돼 있다**: Beat Transformer(ISMIR 2022, MIT) ablation — 스템 정보를 주면 beat F1은 0.9pp 이내로 거의 불변인데 **downbeat F1만 1.1~2.8pp 일관 개선**. Drum-Aware Ensemble도 독립적으로 같은 패턴(downbeat 개선폭이 beat의 2.4배). 단 "베이스만 단독으로 위상"은 아무도 검증하지 않았다.
- **beat_this 후속 논문이 우리 실패를 그대로 기술**: "The SMC Blind Spot"(arXiv 2605.12287) — 느린 템포·루바토에서 "틀린 곳에서 확신하는" 실패, 템포 불안정성과 상관 ρ=−0.305. 해결책으로 소스분리 기반 학습을 **제안만 하고 미구현**.
- **되먹임 고리는 기성품이 있다**: `synctoolbox`(MIT, Meinard Müller 그룹) — MrMsDTW+DLNCO로 MIDI↔오디오 정렬. **우리 환경 설치 확인 완료**(Python 3.12 + numpy 2.4 그대로, dry-run 통과. music21·pandas·numba 동반 설치됨 — music21은 조판 정규화에도 필요하니 일석이조). 측정은 `mir_eval.alignment`(이미 보유)의 `absolute_error`/`percentage_correct`를 그대로 쓰면 된다.
- **재타격 검출 기성품은 없다**: crepe_notes도 madmom 일반 온셋을 threshold 0.7로 재사용하는 수준(= 우리가 이미 실패한 계열). madmom ADSR HMM은 피아노 CNN 활성값 입력이라 베이스에 못 쓴다. essentia는 **Windows Python 바인딩 공식 미지원**(WSL 필요). 참고할 알고리즘은 Tony/pYIN의 RMS 비율 재분절 공식(의존성 0, 10줄).
- **통합 프레임워크는 전부 우리보다 완성도 낮다**: StemScore(star 0), Stem-Separator-AMT(대학 과제) 등은 비트 그리드 문제를 아예 다루지 않는다. Omnizart는 TensorFlow 스택 추가 + 비트-노트 자동 연동 없음.
- **가장 매력적이지만 못 쓰는 것**: Wachter et al. 트랜스포머 리듬 양자화(arXiv 2508.19262) — ASAP에서 **Onset F1 97.3%**인데 공개 코드·가중치가 없다.

### 이전 기록 — 그리드 정렬 (원인 규명 전)
실곡 `beatStability` 0.244 / `quantizationResidual` 0.627 / 품질 56(reference). 마디 시작 대비 최대 진폭 상승 위치를 36마디에서 재보니 **표준편차 0.565초**로 흩어지고, ±0.4~0.8초 양쪽으로 갈린다. 우리 마디선이 음악의 마디에 잠기지 않았다. 25~28마디 슬롯 0 어택이 4마디 모두 비어 있는 것도 이 때문이다.

이 곡 로컬 템포는 0.78~0.90초/박(≈73BPM)인데 median은 75BPM이다. 후보: madmom 비트 트래커(DBN), 또는 단일 전역 템포+위상을 온셋에 피팅해 그리드를 고정(현재는 검출된 개별 비트를 그대로 등분).

## 이번 세션에서 한 것

### 1. 스템 opus 인코딩 배선 (완료·검증됨)
- `apps/worker/pipeline/encode.py` (신규): wav→opus 96k, mtime 캐시. 237MB→16MB(14배)
- `apps/worker/jobs.py`: STAGES에 `encode`(separate 직후) + manifest에 `stemFormat: "opus"`
- `scripts/run_pipeline.py`: CLI 동일 배선 (--skip-separate면 stems/stemFormat 키 생략)
- 기존 data 4개 디렉토리 일괄 마이그레이션 완료(opus 생성 + manifest 패치)
- 웹: `engine.ts`에 `stemUrls(hash, format)` 헬퍼, PlayerShell/SelfTest가 manifest의 `stemFormat`으로 확장자 결정(없으면 wav = 구버전 호환)

### 2. 치명 버그 발견·수정: "재생 버튼 무반응"의 진짜 원인 (완료·검증됨)
wav 용량은 부차적 문제였다. 진짜 원인은 **signalsmith-stretch 1.3.2 벤더 버그**:
- process()의 비활성(inactive) 분기가 `inputs[c%inputs.length]`를 무조건 읽음 → `numberOfInputs: 0`이면 inputs가 undefined → 첫 쿼텀에서 TypeError → **processorerror로 프로세서 영구 사망**
- 실시간 AudioContext는 스케줄 전(비활성)에도 process()가 돌므로 무조건 죽음. OfflineAudioContext는 startRendering 시점에 이미 active라 안 걸림 → SelfTest만 통과했던 이유
- 수정: `apps/web/scripts/sync-vendor.mjs`가 복사 후 해당 죽은 코드를 자동 패치. **패턴 미발견 시 빌드 실패**하게 해서 업스트림 업데이트 때 재검토 강제
- **`public/vendor/`는 생성물(패치 포함). 직접 수정 금지 — sync-vendor.mjs가 소스**

### 3. engine.ts 버그 수정 3건
- `addBuffers` 후 `this.channels = []` 해제 — 디코딩 PCM(5분 곡 ~474MB) 메인 스레드 사본 제거
- `_pendingSeek`: 그래프 생성 전 seek가 조용히 유실되던 버그(alphaTab이 렌더 직후 seekTo를 보냄) → 첫 play에서 반영
- `stretch.start()` 호출 제거(play/prepareOffline): 벤더 start()는 `schedule({input:0,...})` 설탕이라 **일시정지 후 재개 때마다 곡 처음으로 되감던 버그**. 실측: resumedAdvance -1.24로 확인 → 수정. **수정 후 재개 동작 재측정은 아직 안 함**

### 4. SelfTest 개선 (완료·검증됨)
- 검증 구간을 첫 6초 고정 → **곡 중간 지점**으로: 실곡은 인트로에 베이스가 없어(이 곡은 24마디 쉼표) 솔로 검증이 헛돌던 설계 결함
- render()가 urls를 받도록 변경 + `prepareOffline(rate, startSec)` 추가
- 실곡 975e에서 6/6 PASS 확인 (베이스 솔로 RMS 0.4773 = 전체의 96%)

### 5. 커서 + 조판 (코드 반영, **미검증**)
- **커서**: 재생 제어를 alphaTab 경유로 변경. `ScoreView`가 `controlRef`(ScoreControl)를 채우고, PlayerShell의 toggle이 `api.play()/pause()`를 호출 → alphaTab이 handler.play()로 엔진을 켬. alphaTab 자신이 "재생 중" 상태여야 커서를 그리기 때문. 악보 없으면 엔진 직접 제어 폴백
- **조판**: `StaveProfile.ScoreTab`(오선보+TAB 병기), `display.barsPerRow: 4`(행당 마디 고정), `notation.rhythmMode: TabRhythmMode.ShowWithBars`(TAB 아래 리듬 기둥) — enum 실측 확인함

## 검증 완료 목록
| 항목 | 결과 |
|---|---|
| CLI 파이프라인 재실행(픽스처) | encode 캐시 포함 정상, quality 73(good) |
| 기존 4 dir opus 마이그레이션 | 완료, manifest에 stemFormat 반영 |
| next build | **최종 변경분(커서·조판·engine 수정 포함) 전부 통과** (exit 0) |
| SelfTest(오프라인) 실곡 975e | 6/6 PASS |
| 실시간 재생(픽스처, 벤더 패치 후) | advance1x 2.03 / half-rate 0.98 / seek 정상 / pause drift 0 |
| 실시간 재개(resume) | 버그 발견(-1.24) → start() 제거로 수정, **재측정 필요** |

## 다음 세션 확인 목록 (우선순위)

1. **빌드 + 브라우저 재검증**: `npm run build` 결과 확인 → dev 서버 → `/play/6747c56c4a043f8a`(16s 픽스처), `/play/975e4e588d282666`(실곡)에서
   - 재생/일시정지/**재개**(0초 되감김 없어야)/시킹
   - **커서가 악보 위에서 움직이는지** (이번 변경의 핵심 검증)
   - 새 조판(오선+TAB, 4마디/행, 리듬 기둥)이 실제로 적용됐는지 스크린샷
   - SelfTest 두 hash 모두 PASS 유지
2. **비트 추적 개선 (악보 품질의 남은 병목)**:
   - 실곡에서 `bpm_variance` 0.076, 비트간격 0.180~1.460s로 beat_this가 실패한다. 이것 때문에 양자화 잔차가 크고, 스윙 게이트도 이 값에 의존하고 있다.
   - PRD 5의 대안 `madmom`(비상업 라이선스라 개인용 사용 가능) 전환 검토. 또는 beat_this 후처리(DBN) 추가.
   - 개선되면 스윙 게이트 임계(0.05)를 재검토해야 한다.
3. **골든셋 확보 (다른 모든 튜닝의 선행조건)**:
   - `eval/golden/`이 없다. 현재 자산은 16초 합성 픽스처 3종뿐이고 너무 깨끗해서 실곡 실패모드를 못 잡는다(병합 오작동 가설을 픽스처로 검증할 수 없었던 이유).
   - 누출 필터 상수(14반음/0.6/0.85)와 스윙 게이트(0.05)는 실곡 1건 + 픽스처 3종으로 정한 값이다. 골든셋 없이 추가 튜닝하면 과적합된다.
4. **채보 엔진 전환 검토 (리서치 근거 있음)**:
   - FiloBass 벤치마크(분리된 베이스 스템, 50ms): basic-pitch P 51.4/R 81.3/F 62.7 vs **CREPE Notes P 71.8/R 74.1/F 72.9**. basic-pitch는 polyphonic 아키텍처라 구조적으로 과검출한다.
   - `crepe_notes`(백엔드 torchcrepe/PESTO 교체 가능). madmom 의존성은 Python 3.12에서 git 직접 설치 우회 필요.
   - **스템 분리 교체는 우선순위 아니다**: 최선 후보가 bass SDR 11.76→11.94(+0.18dB)뿐. BS-Roformer-SW는 14.62지만 CPU 5분곡 ~34분 추정으로 예산 초과.
5. **조판 정규화 (표기 품질 다듬기)**:
   - AlphaTex 타이 `-.8`/`-.16`을 막는 코드는 파서 소스에 **없다**. `{t}`/`{-}` 동등 대체 문법이 공식 문서에 있으니 먼저 이걸로 재시도.
   - 박 경계 분할+타이는 music21 `makeTies`/`splitAtDurations` 또는 abjad `rewrite_meter`가 이미 푼 문제. 양자화 뒤·AlphaTex 생성 앞에 정규화 단계로 끼우는 게 정석.
6. **웹 워커 경로 encode 검증**: 새 유튜브 잡 1건 돌려 encode 스테이지가 SSE 진행률에 뜨고 opus가 생기는지 (지금까지는 CLI·마이그레이션으로만 확인)
7. dev에서 리로드 직후 자동재생처럼 보이는 현상 1회 관찰(Fast Refresh 이중 마운트 잔상 추정) — 프로덕션 빌드에서 재확인

## 실행 메모
- 워커: `cd apps/worker && ../../.venv/Scripts/python -m uvicorn main:app --port 8000`
- 웹: `cd apps/web && npm run dev` (predev가 sync-vendor 자동 실행 → 벤더 패치도 자동 적용)
- 검증기: `cd apps/web && node ../../tools/validate_alphatex.mjs <file>` (모듈 해석 때문에 apps/web에서)
- 실시간 재생 디버깅: 플레이어 페이지 콘솔에서 `window.__player` (dev 전용 훅)
